import type { Tool } from '../../types/agent-connectors'

export const tools: Tool[] = [
  {
    name: 'lushamcp_account_usage',
    description: `Retrieve account credit balance, rate-limit status, plan info, and per-action credit pricing.`,
    params: [
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_companies_search',
    description: `Look up known companies in the Lusha database by name, domain, or FQDN, supporting batches of up to 25.`,
    params: [
      { name: 'companies', type: 'array', required: true, description: `No description.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'enrich', type: 'boolean', required: false, description: `Auto-enrich the results. When true (default) the response includes firmographics, technologies and intent in one call and consumes reveal credits. When false only the search preview is returned (no premium fields, no reveal credits); follow up with prospecting_company_enrich on the returned ids when reveals are needed.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_contacts_search',
    description: `Look up a known business contact in Lusha by name, company, LinkedIn URL, or email.`,
    params: [
      { name: 'company_name', type: 'string', required: false, description: `Company name the contact works at. Required when looking up by name (first_name + last_name + company_name).` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'email', type: 'string', required: false, description: `Contact's email address. Standalone lookup path.` },
      { name: 'enrich', type: 'boolean', required: false, description: `Auto-enrich the result. When true (default) the response includes emails and phones in one call and consumes reveal credits. When false only the search preview is returned (no emails/phones, no reveal credits); follow up with prospecting_contact_enrich on the returned id when reveals are needed.` },
      { name: 'first_name', type: 'string', required: false, description: `Contact's first name. Use with last_name + company_name as one of the supported lookup paths.` },
      { name: 'last_name', type: 'string', required: false, description: `Contact's last name. Use with first_name + company_name as one of the supported lookup paths.` },
      { name: 'linkedin_url', type: 'string', required: false, description: `Contact's LinkedIn profile URL. Standalone lookup path.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_lookalike_companies',
    description: `Discover companies similar to a set of seed companies, returning paginated lookalike candidates.`,
    params: [
      { name: 'seeds', type: 'object', required: true, description: `Seed companies to find lookalikes for. Total unique identifiers across both arrays must be 5-100.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'dedupeSessionId', type: 'string', required: false, description: `Opaque pagination/dedupe token (UUID). Omit on first call to start a new run; the response includes a dedupeSessionId to pass back on subsequent calls for the same seeds. Sessions expire after 30 days of inactivity.` },
      { name: 'exclude', type: 'object', required: false, description: `Optional companies to exclude from results (e.g., existing customers). Total identifiers max 500. Distinct from dedupeSessionId, which excludes companies already returned by prior pagination calls.` },
      { name: 'limit', type: 'integer', required: false, description: `Number of results per call (1-100). Defaults to 25 when omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_lookalike_contacts',
    description: `Discover contacts similar to a set of seed contacts, returning paginated lookalike candidates.`,
    params: [
      { name: 'seeds', type: 'object', required: true, description: `Seed contacts to find lookalikes for. Total unique identifiers across all arrays must be 5-100.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'dedupeSessionId', type: 'string', required: false, description: `Opaque pagination/dedupe token (UUID). Omit on first call to start a new run; the response includes a dedupeSessionId to pass back on subsequent calls for the same seeds. Sessions expire after 30 days of inactivity.` },
      { name: 'exclude', type: 'object', required: false, description: `Optional contacts to exclude from results (e.g., existing customers). Total identifiers max 500. Distinct from dedupeSessionId, which excludes contacts already returned by prior pagination calls.` },
      { name: 'limit', type: 'integer', required: false, description: `Number of results per call (1-50). Defaults to 25 when omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_prospecting_company_enrich',
    description: `Reveal full firmographic details for one or more Lusha company IDs.`,
    params: [
      { name: 'ids', type: 'array', required: true, description: `Lusha company IDs to enrich (from prospecting_company_search or companies_search results). Each id is a numeric string. Capped at 25 per request — split larger batches to fit the 25k-token response budget.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'reveal', type: 'array', required: false, description: `Company premium fields to reveal. Source of truth: the prior search response's \`canReveal[].field\` (typically a subset of 'employeesByDepartment', 'employeesByLocation', 'employeesBySeniority', 'competitors', 'intent'); per-field credit cost for this account is in \`canReveal[].credits\`. When omitted, V3 returns only the free 'intent' field — chargeable firmographics require an explicit list of fields. Values not present in the prior response's \`canReveal\` are rejected by V3 — \`canReveal\` reflects this account's entitlements and per-company data availability.` },
    ],
  },
  {
    name: 'lushamcp_prospecting_company_filters',
    description: `Resolve valid filter values accepted by the company prospecting search.`,
    params: [
      { name: 'type', type: 'string', required: true, description: `Type of Lusha company filter to resolve. Large result types (naics, sics) return all available values; pass 'q' to narrow names, locations, and technologies.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'q', type: 'string', required: false, description: `Narrowing query. Required for: names, locations, technologies. Ignored for other types.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_prospecting_company_search',
    description: `Find companies by firmographic filters such as industry, size, location, and technology.`,
    params: [
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'domains', type: 'array', required: false, description: `Lusha prospecting: company domains to include (e.g., 'lusha.com').` },
      { name: 'exclude', type: 'object', required: false, description: `Optional filters that exclude matching companies. Mirrors the include-side filters. Signals are not supported on the exclude side.` },
      { name: 'intentTopics', type: 'array', required: false, description: `Lusha prospecting: intent topics. Use values from prospecting_company_filters (type='intent_topics').` },
      { name: 'locations', type: 'array', required: false, description: `Lusha prospecting: company locations. Use values from prospecting_company_filters (type='locations').` },
      { name: 'mainIndustriesIds', type: 'array', required: false, description: `Lusha prospecting: main industry IDs. Use values from prospecting_company_filters (type='industries_labels').` },
      { name: 'naicsCodes', type: 'array', required: false, description: `Lusha prospecting: NAICS codes. Use values from prospecting_company_filters (type='naics').` },
      { name: 'names', type: 'array', required: false, description: `Lusha prospecting: company names to include. Use values from prospecting_company_filters (type='names').` },
      { name: 'page', type: 'integer', required: false, description: `Page number for pagination (0-based).` },
      { name: 'page_size', type: 'integer', required: false, description: `Items per page (min 10, max 50, default 20).` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'revenues', type: 'array', required: false, description: `Lusha prospecting: revenue ranges (annual USD). Use values from prospecting_company_filters (type='revenues').` },
      { name: 'searchText', type: 'string', required: false, description: `Lusha prospecting: free-text relevance hint across company data. Layered on top of structured filters; not an exact match - broad or unrelated text can still return a large company population. Prefer structured filters (industries, sizes, locations, technologies) for precision.` },
      { name: 'sicsCodes', type: 'array', required: false, description: `Lusha prospecting: SIC codes. Use values from prospecting_company_filters (type='sics').` },
      { name: 'signals', type: 'object', required: false, description: `Premium filter: narrow results to companies with recent signal activity. Charges Lusha credits per signal type that returns results, in addition to base prospecting credits.` },
      { name: 'sizesFilterOption', type: 'array', required: false, description: `Lusha prospecting: company size ranges (employee count). Valid min/max ranges are returned by prospecting_company_filters (type='sizes'); custom ranges are not supported.` },
      { name: 'subIndustriesIds', type: 'array', required: false, description: `Lusha prospecting: sub industry IDs. Use values from prospecting_company_filters (type='industries_labels').` },
      { name: 'technologies', type: 'array', required: false, description: `Lusha prospecting: technologies. Use values from prospecting_company_filters (type='technologies').` },
    ],
  },
  {
    name: 'lushamcp_prospecting_contact_enrich',
    description: `Reveal emails and phone numbers for one or more Lusha contact IDs.`,
    params: [
      { name: 'ids', type: 'array', required: true, description: `Lusha contact IDs to enrich (from prospecting_contact_search or contacts_search results). Each id is a numeric string. Capped at 50 per request — split larger batches to fit the 25k-token response budget.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'reveal', type: 'array', required: false, description: `Contact premium fields to reveal. Source of truth: the prior search response's \`canReveal[].field\` (typically 'emails', 'phones'); per-field credit cost for this account is in \`canReveal[].credits\`. When omitted, V3 returns every field listed in each contact's \`canReveal\`. Values not present in the prior response's \`canReveal\` are rejected by V3 — \`canReveal\` reflects this account's entitlements and per-contact data availability.` },
    ],
  },
  {
    name: 'lushamcp_prospecting_contact_filters',
    description: `Resolve valid filter values accepted by the contact prospecting search.`,
    params: [
      { name: 'type', type: 'string', required: true, description: `Type of Lusha contact filter to resolve.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'locationSearchText', type: 'string', required: false, description: `Narrowing text for location-based searches. Required when type='locations'.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_prospecting_contact_search',
    description: `Find business contacts by filters such as job title, seniority, department, and company attributes.`,
    params: [
      { name: 'companyDomains', type: 'array', required: false, description: `Lusha prospecting: filter by company domains (e.g., 'lusha.com').` },
      { name: 'companyLocations', type: 'array', required: false, description: `Lusha prospecting: filter by company locations. Use values from prospecting_company_filters (type='locations').` },
      { name: 'companyNames', type: 'array', required: false, description: `Lusha prospecting: filter by company names.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'countries', type: 'array', required: false, description: `Lusha prospecting: ISO-2 country codes. Use values from prospecting_contact_filters (type='all_countries').` },
      { name: 'departments', type: 'array', required: false, description: `Lusha prospecting: department names. Use values from prospecting_contact_filters (type='departments').` },
      { name: 'exclude', type: 'object', required: false, description: `Optional filters that exclude matching contacts/companies. Mirrors the include-side filters; any contact/company matching any exclude filter is dropped. Signals are not supported on the exclude side.` },
      { name: 'existing_data_points', type: 'array', required: false, description: `Lusha prospecting: required data types on contacts. Use values from prospecting_contact_filters (type='existing_data_points').` },
      { name: 'intentTopics', type: 'array', required: false, description: `Lusha prospecting: filter by company intent topics. Use values from prospecting_company_filters (type='intent_topics').` },
      { name: 'jobTitles', type: 'array', required: false, description: `Lusha prospecting: contact job titles (free-form strings, e.g., 'VP of Sales').` },
      { name: 'locations', type: 'array', required: false, description: `Lusha prospecting: contact locations. Use values from prospecting_contact_filters (type='locations').` },
      { name: 'mainIndustriesIds', type: 'array', required: false, description: `Lusha prospecting: main industry IDs. Use values from prospecting_company_filters (type='industries_labels').` },
      { name: 'naicsCodes', type: 'array', required: false, description: `Lusha prospecting: NAICS codes. Use values from prospecting_company_filters (type='naics').` },
      { name: 'names', type: 'array', required: false, description: `Lusha prospecting: contact names to include.` },
      { name: 'page', type: 'integer', required: false, description: `Page number for pagination (0-based) - use with page_size.` },
      { name: 'page_size', type: 'integer', required: false, description: `Items per page (min 10, max 50, default 20) - use with page.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'revenues', type: 'array', required: false, description: `Lusha prospecting: company revenue ranges (annual USD). Use values from prospecting_company_filters (type='revenues').` },
      { name: 'searchText', type: 'string', required: false, description: `Lusha prospecting: free-text relevance hint across contact data. Layered on top of structured filters; not an exact match - broad or unrelated text can still return a large contact population. Prefer structured filters (jobTitles, seniority, departments, countries) for precision.` },
      { name: 'seniority', type: 'array', required: false, description: `Lusha prospecting: seniority level IDs. Use values from prospecting_contact_filters (type='seniority').` },
      { name: 'sicsCodes', type: 'array', required: false, description: `Lusha prospecting: SIC codes. Use values from prospecting_company_filters (type='sics').` },
      { name: 'signals', type: 'object', required: false, description: `Premium filter: narrow results to contacts with recent signal activity. Charges Lusha credits per signal type that returns results, in addition to base prospecting credits.` },
      { name: 'sizesFilterOption', type: 'array', required: false, description: `Lusha prospecting: company size ranges (employee count). Valid min/max ranges are returned by prospecting_company_filters (type='sizes').` },
      { name: 'subIndustriesIds', type: 'array', required: false, description: `Lusha prospecting: sub industry IDs. Use values from prospecting_company_filters (type='industries_labels').` },
      { name: 'technologies', type: 'array', required: false, description: `Lusha prospecting: filter by company technologies. Use values from prospecting_company_filters (type='technologies').` },
    ],
  },
  {
    name: 'lushamcp_prospecting_search_guide',
    description: `Return a step-by-step guide for structuring Lusha prospecting searches.`,
    params: [
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'target', type: 'string', required: false, description: `Target entity type for Lusha search guidance (companies or contacts). If not specified, provides guidance for both.` },
    ],
  },
  {
    name: 'lushamcp_signals_companies_get',
    description: `Return recent activity signals (hiring, headcount, IT spend, news) for known Lusha company IDs.`,
    params: [
      { name: 'companyIds', type: 'array', required: true, description: `Lusha company IDs (1-25 per request). Capped at 25 to fit the 25k-token response budget once signal events are merged; split larger lists into multiple calls.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'filters', type: 'object', required: false, description: `Optional filters narrowing news/hiring company signal results. Pair with 'riskNews', 'commercialActivityNews', etc. (newsEventTypes) and 'surgeInHiringByDepartment' / 'surgeInHiringByLocation' (hiringByDepartments / hiringByLocations).` },
      { name: 'maxResultsPerSignal', type: 'integer', required: false, description: `Maximum number of signal instances per signal type per company (1-100). Defaults to 10 when omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'signals', type: 'array', required: false, description: `Deprecated: use 'signalTypes'. Kept for backward compatibility; will be removed in a future release.` },
      { name: 'signalTypes', type: 'array', required: false, description: `Company signal type identifiers (e.g., 'surgeInHiring', 'headcountIncrease3m', 'riskNews'); 'allSignals' selects every supported type. Source of truth: signals_company_filters - resolve valid values from there before calling. Invalid values return a 400 from the Lusha API.` },
      { name: 'startDate', type: 'string', required: false, description: `Start date for signal retrieval (YYYY-MM-DD). Must be a valid calendar date and not in the future. Defaults to last 6 months when omitted.` },
    ],
  },
  {
    name: 'lushamcp_signals_companies_search',
    description: `Resolve companies by domain or name and return their recent activity signals.`,
    params: [
      { name: 'companies', type: 'array', required: true, description: `Companies to identify and enrich with signals (1-25 per request). Capped at 25 to fit the 25k-token response budget once signal events are merged.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'filters', type: 'object', required: false, description: `Optional filters narrowing news/hiring company signal results. Pair with 'riskNews', 'commercialActivityNews', etc. (newsEventTypes) and 'surgeInHiringByDepartment' / 'surgeInHiringByLocation' (hiringByDepartments / hiringByLocations).` },
      { name: 'maxResultsPerSignal', type: 'integer', required: false, description: `Maximum number of signal instances per signal type per company (1-100). Defaults to 10 when omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'signals', type: 'array', required: false, description: `Deprecated: use 'signalTypes'. Kept for backward compatibility; will be removed in a future release.` },
      { name: 'signalTypes', type: 'array', required: false, description: `Company signal type identifiers (e.g., 'surgeInHiring', 'headcountIncrease3m', 'riskNews'); 'allSignals' selects every supported type. Source of truth: signals_company_filters - resolve valid values from there before calling. Invalid values return a 400 from the Lusha API.` },
      { name: 'startDate', type: 'string', required: false, description: `Start date for signal retrieval (YYYY-MM-DD). Must be a valid calendar date and not in the future. Defaults to last 6 months when omitted.` },
    ],
  },
  {
    name: 'lushamcp_signals_company_filters',
    description: `Discover available company signal types and filter values for signals searches.`,
    params: [
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'filterType', type: 'string', required: false, description: `Optional. Omit to discover signal types and the directory of available filter types. Set to fetch values for a single filter (e.g., 'newsEventTypes', 'hiringByDepartments', 'hiringByLocations').` },
      { name: 'query', type: 'string', required: false, description: `Search text. Required when filterType is 'hiringByLocations' (locations are not enumerable). Not supported for other filterTypes or when filterType is omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_signals_contact_filters',
    description: `Return available contact signal types accepted by contacts signals tools.`,
    params: [
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
    ],
  },
  {
    name: 'lushamcp_signals_contacts_get',
    description: `Return recent activity signals (promotions, company changes) for known Lusha contact IDs.`,
    params: [
      { name: 'contactIds', type: 'array', required: true, description: `Lusha contact IDs (1-25 per request). Capped at 25 to fit the 25k-token response budget once signal events are merged; split larger lists into multiple calls.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'maxResultsPerSignal', type: 'integer', required: false, description: `Maximum number of signal instances per signal type per contact (1-100). Defaults to 10 when omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'signals', type: 'array', required: false, description: `Deprecated: use 'signalTypes'. Kept for backward compatibility; will be removed in a future release.` },
      { name: 'signalTypes', type: 'array', required: false, description: `Contact signal type identifiers (e.g., 'allSignals', 'promotion', 'companyChange'). Source of truth: signals_contact_filters - resolve valid values from there before calling. Invalid values return a 400 from the Lusha API.` },
      { name: 'startDate', type: 'string', required: false, description: `Start date for signal retrieval (YYYY-MM-DD). Must be a valid calendar date and not in the future. Defaults to last 6 months when omitted.` },
    ],
  },
  {
    name: 'lushamcp_signals_contacts_search',
    description: `Resolve contacts by LinkedIn URL, email, or name and return their recent activity signals.`,
    params: [
      { name: 'contacts', type: 'array', required: true, description: `Contacts to identify and enrich with signals (1-25 per request). Capped at 25 to fit the 25k-token response budget once signal events are merged.` },
      { name: 'conversation_id', type: 'string', required: false, description: `Conversation correlation ID. Present only when an earlier tool response in this conversation returned one; that value is carried unchanged on subsequent calls. Omitted on the first call.` },
      { name: 'maxResultsPerSignal', type: 'integer', required: false, description: `Maximum number of signal instances per signal type per contact (1-100). Defaults to 10 when omitted.` },
      { name: 'reason_for_invocation', type: 'string', required: false, description: `Brief explanation of why you chose this tool for the current task. Optional audit field; max 500 characters (longer values are truncated). Plain text only.` },
      { name: 'signals', type: 'array', required: false, description: `Deprecated: use 'signalTypes'. Kept for backward compatibility; will be removed in a future release.` },
      { name: 'signalTypes', type: 'array', required: false, description: `Contact signal type identifiers (e.g., 'allSignals', 'promotion', 'companyChange'). Source of truth: signals_contact_filters - resolve valid values from there before calling. Invalid values return a 400 from the Lusha API.` },
      { name: 'startDate', type: 'string', required: false, description: `Start date for signal retrieval (YYYY-MM-DD). Must be a valid calendar date and not in the future. Defaults to last 6 months when omitted.` },
    ],
  },
]
