import type { Tool } from '../../types/agent-connectors'

export const tools: Tool[] = [
  {
    name: 'gocardlessmcp_cancel_mandate',
    description: `Cancel a mandate (Direct Debit authorisation). This also auto-cancels any active subscriptions and pending payments attached to the mandate. Irreversible once cancelled.`,
    params: [
      {
        name: 'confirmed',
        type: 'boolean',
        required: false,
        description: `Set true only after the user has explicitly confirmed the preview in their most recent message. Default false (returns a preview).`,
      },
      {
        name: 'mandate_id',
        type: 'string',
        required: false,
        description: `ID of the mandate to cancel (e.g. MD000123). Must be in a cancellable state — see description.`,
      },
      {
        name: 'mandate_ids',
        type: 'array',
        required: false,
        description: `For bulk cancellations: list of mandate IDs to cancel (max 10). Use instead of mandate_id.`,
      },
      {
        name: 'preview_token',
        type: 'string',
        required: false,
        description: `Required when confirmed=true: the preview_token field from this tool's own preceding preview response (the confirmed=false call for these exact same parameters). Rejected if missing, mismatched, already used, or if confirmed=true arrives too soon after the preview — a preview_token is only valid a few seconds after being issued, since it exists to prove a genuine user reply happened in between, not just that a preview call was made. If confirmed=true is rejected for being too soon, wait for the user's actual next message before trying again; do not immediately retry.`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_cancel_payment',
    description: `Cancel a payment before it is submitted to the bank. Only payments in pending_customer_approval or pending_submission state can be cancelled. Irreversible once cancelled.`,
    params: [
      {
        name: 'confirmed',
        type: 'boolean',
        required: false,
        description: `Set true only after the user has explicitly confirmed the preview in their most recent message. Default false (returns a preview).`,
      },
      {
        name: 'payment_id',
        type: 'string',
        required: false,
        description: `ID of the payment to cancel (e.g. PM000123). Must be in pending_customer_approval or pending_submission state.`,
      },
      {
        name: 'payment_ids',
        type: 'array',
        required: false,
        description: `For bulk cancellations: list of payment IDs to cancel (max 10). Use instead of payment_id.`,
      },
      {
        name: 'preview_token',
        type: 'string',
        required: false,
        description: `Required when confirmed=true: the preview_token field from this tool's own preceding preview response (the confirmed=false call for these exact same parameters). Rejected if missing, mismatched, already used, or if confirmed=true arrives too soon after the preview — a preview_token is only valid a few seconds after being issued, since it exists to prove a genuine user reply happened in between, not just that a preview call was made. If confirmed=true is rejected for being too soon, wait for the user's actual next message before trying again; do not immediately retry.`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_create_payment',
    description: `Create a one-off payment against an existing mandate. The mandate must be active or pending submission, and the payment currency must match the mandate's currency.`,
    params: [
      {
        name: 'amount',
        type: 'number',
        required: true,
        description: `Amount in minor units (e.g. pence for GBP, cents for USD).`,
      },
      {
        name: 'currency',
        type: 'string',
        required: true,
        description: `ISO 4217 currency code. Must match the mandate's currency.`,
      },
      {
        name: 'mandate_id',
        type: 'string',
        required: true,
        description: `ID of the mandate to charge (e.g. MD000123). Must be in pending_submission, submitted, or active state.`,
      },
      {
        name: 'charge_date',
        type: 'string',
        required: false,
        description: `Optional charge date (YYYY-MM-DD). Omit to use the next possible charge date for the mandate.`,
      },
      {
        name: 'confirmed',
        type: 'boolean',
        required: false,
        description: `Set true only after the user has explicitly confirmed the preview in their most recent message. Default false (returns a preview).`,
      },
      {
        name: 'count',
        type: 'number',
        required: false,
        description: `Number of identical payments to create against the same mandate (max 25, default 1). When >1, returns a bulk preview before executing.`,
      },
      {
        name: 'description',
        type: 'string',
        required: false,
        description: `Optional name for the payment shown in the GoCardless dashboard and on payer confirmation emails. Helps merchants and payers identify the payment (e.g. "Admin fee", "Invoice 123").`,
      },
      {
        name: 'faster_ach',
        type: 'boolean',
        required: false,
        description: `ACH only. Set true to explicitly use Faster ACH, false for standard ACH. Omit to let the presence or absence of charge_date determine the processing speed.`,
      },
      {
        name: 'preview_token',
        type: 'string',
        required: false,
        description: `Required when confirmed=true: the preview_token field from this tool's own preceding preview response (the confirmed=false call for these exact same parameters). Rejected if missing, mismatched, already used, or if confirmed=true arrives too soon after the preview — a preview_token is only valid a few seconds after being issued, since it exists to prove a genuine user reply happened in between, not just that a preview call was made. If confirmed=true is rejected for being too soon, wait for the user's actual next message before trying again; do not immediately retry.`,
      },
      {
        name: 'reference',
        type: 'string',
        required: false,
        description: `Custom bank statement reference. Scheme-specific length limits apply (e.g. 10 chars for Bacs, 140 for SEPA). Not supported for Faster Payments. Bacs custom references require GoCardless Plus/Pro/Enterprise.`,
      },
      {
        name: 'retry_if_possible',
        type: 'boolean',
        required: false,
        description: `Automatically retry the payment on failure using intelligent retries. Requires Success+ to be enabled in the GoCardless dashboard. Default false.`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_create_payment_link',
    description: `Create a Billing Request — a single-use GoCardless-hosted authorisation link for a specific payer, supporting mandate setup, one-off IBP payments, or VRP consent.`,
    params: [
      {
        name: 'scheme',
        type: 'string',
        required: true,
        description: `Payment scheme for the mandate (or for IBP when payment_only=true). Examples: faster_payments, bacs, sepa_core, ach, pad, becs, pay_to.`,
      },
      {
        name: 'amount',
        type: 'number',
        required: false,
        description: `Amount in minor units (e.g. pence for GBP). Required when payment_only=true.`,
      },
      {
        name: 'constraints_end_date',
        type: 'string',
        required: false,
        description: `Latest date by which payments can be taken (YYYY-MM-DD). Leave unset for an open-ended agreement.`,
      },
      {
        name: 'constraints_max_amount_per_payment',
        type: 'number',
        required: false,
        description: `Maximum amount in minor units that can be charged per payment. Required for PayTo and VRP mandates.`,
      },
      {
        name: 'constraints_start_date',
        type: 'string',
        required: false,
        description: `Earliest date from which payments can be taken (YYYY-MM-DD). Defaults to the day the payer authorises.`,
      },
      {
        name: 'currency',
        type: 'string',
        required: false,
        description: `ISO 4217 currency code. Required when amount is set.`,
      },
      {
        name: 'customer_bank_account_id',
        type: 'string',
        required: false,
        description: `Existing GoCardless customer bank account ID to pre-fill, skipping the bank account collection step.`,
      },
      {
        name: 'customer_id',
        type: 'string',
        required: false,
        description: `Existing GoCardless customer ID to link, optional.`,
      },
      {
        name: 'description',
        type: 'string',
        required: false,
        description: `Description shown to the payer during authorisation.`,
      },
      {
        name: 'fallback_enabled',
        type: 'boolean',
        required: false,
        description: `If true, the billing request can fall back to Direct Debit if the payer's bank does not support the requested scheme (IBP or VRP). Applies to both IBP payment requests and VRP mandate requests. Do not set if GoCardless payment intelligence is enabled.`,
      },
      {
        name: 'mandate_verify',
        type: 'string',
        required: false,
        description: `Verification preference for the mandate: minimum, recommended (default), when_available, or always.`,
      },
      {
        name: 'payment_context_code',
        type: 'string',
        required: false,
        description: `Payment context for cVRP mandates. One of: billing_goods_and_services_in_advance, billing_goods_and_services_in_arrears, pisp_payee, ecommerce_merchant_initiated_payment, face_to_face_point_of_sale, transfer_to_self, transfer_to_third_party, bill_payment, ecommerce_goods, ecommerce_services, other, party_to_party. Required for cVRP.`,
      },
      {
        name: 'payment_only',
        type: 'boolean',
        required: false,
        description: `Set true for a payment-only (IBP) flow — no mandate will be created. Requires amount and currency.`,
      },
      {
        name: 'payment_purpose_code',
        type: 'string',
        required: false,
        description: `Underlying payment purpose for cVRP mandates (e.g. loan_repayment, bank_loan_delayed_draw_funding, salary_payment). See VRP Commercial Payment Purpose Codes for the full list. Required for cVRP.`,
      },
      {
        name: 'payment_request_scheme',
        type: 'string',
        required: false,
        description: `Override the IBP scheme used for the payment_request in a combined flow. Omit to let the API infer from currency. Only valid when amount is set and payment_only=false.`,
      },
      {
        name: 'periodic_limit_alignment',
        type: 'string',
        required: false,
        description: `How the period resets: calendar (end of current period, e.g. end of month) or creation_date (same date next period). Defaults to creation_date.`,
      },
      {
        name: 'periodic_limit_max_total_amount',
        type: 'number',
        required: false,
        description: `Maximum total amount in minor units that can be charged within the periodic_limit_period. Required for VRP mandates.`,
      },
      {
        name: 'periodic_limit_period',
        type: 'string',
        required: false,
        description: `Period for the VRP periodic limit. One of: intraday, day, week, fortnight, month, quarter, half_year, year, flexible. Required when setting a periodic limit.`,
      },
      {
        name: 'purpose_code',
        type: 'string',
        required: false,
        description: `High-level purpose category for the mandate and/or payment. Common values: loan, salary_payment, supplier_payment, tax_payment, commercial, consumer, dividend, pension_payment. See Billing Request Purpose Codes for the full list.`,
      },
      {
        name: 'redirect_uri',
        type: 'string',
        required: false,
        description: `URL to redirect the payer to after they complete the GoCardless-hosted flow. This is the post-flow landing page — not the bank authorisation redirect used in CPP flows. Omit to use the GoCardless default success screen.`,
      },
      {
        name: 'sweeping',
        type: 'boolean',
        required: false,
        description: `Set true for sVRP (Sweeping VRP) — money moved between accounts owned by the same payer. Only valid for faster_payments mandate-only flows; do not combine with payment_only=true.`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_create_payment_template_link',
    description: `Create a reusable Billing Request Template — a permanent shareable link that can be sent to multiple customers, each visit creating a new authorisation session.`,
    params: [
      {
        name: 'name',
        type: 'string',
        required: true,
        description: `Name for this template shown in the dashboard (max 255 chars).`,
      },
      {
        name: 'mandate_request_currency',
        type: 'string',
        required: false,
        description: `ISO 4217 currency code for the mandate (e.g. GBP, EUR). Usually inferred from scheme.`,
      },
      {
        name: 'mandate_request_description',
        type: 'string',
        required: false,
        description: `Description shown to the customer during mandate setup.`,
      },
      {
        name: 'mandate_request_scheme',
        type: 'string',
        required: false,
        description: `Scheme for the mandate. Supported: bacs, sepa_core, ach, pad, becs, becs_nz, autogiro, betalingsservice, pay_to, sepa_credit_transfer. Note: faster_payments is blocked (use create_payment_link for VRP).`,
      },
      {
        name: 'mandate_request_verify',
        type: 'string',
        required: false,
        description: `Verification preference for the mandate: minimum, recommended (default), when_available, or always.`,
      },
      {
        name: 'metadata',
        type: 'object',
        required: false,
        description: `Key-value store of custom data for the template. Up to 3 keys, key names up to 50 chars, values up to 500 chars.`,
      },
      {
        name: 'payment_request_amount',
        type: 'string',
        required: false,
        description: `Amount for the one-off payment in full decimal form (e.g. "25.00"), NOT minor units. Required when payment_request_scheme is set.`,
      },
      {
        name: 'payment_request_currency',
        type: 'string',
        required: false,
        description: `ISO 4217 currency code for the one-off payment. Required when payment_request_amount is set.`,
      },
      {
        name: 'payment_request_description',
        type: 'string',
        required: false,
        description: `Description shown to the customer during payment authorisation.`,
      },
      {
        name: 'payment_request_scheme',
        type: 'string',
        required: false,
        description: `Scheme for the one-off payment. Limited to: faster_payments, sepa_credit_transfer, sepa_instant_credit_transfer. pay_to is not supported.`,
      },
      {
        name: 'redirect_uri',
        type: 'string',
        required: false,
        description: `URL to redirect the customer to after they complete the flow. Omit to use the GoCardless default success screen.`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_create_refund',
    description: `Refund all or part of a previously-collected payment back to the payer's bank account. The payment must be in a refundable state (confirmed or paid_out).`,
    params: [
      {
        name: 'amount',
        type: 'number',
        required: true,
        description: `Refund amount in minor units (e.g. pence for GBP). Must be <= the payment's remaining refundable amount.`,
      },
      {
        name: 'confirmed',
        type: 'boolean',
        required: false,
        description: `Set true only after the user has explicitly confirmed the preview in their most recent message. Default false (returns a preview).`,
      },
      {
        name: 'metadata',
        type: 'object',
        required: false,
        description: `Key-value store of custom data attached to the refund. Up to 3 keys, key names up to 50 chars, values up to 500 chars.`,
      },
      {
        name: 'payment_id',
        type: 'string',
        required: false,
        description: `ID of the payment to refund (e.g. PM000123). Must be in confirmed or paid_out status.`,
      },
      {
        name: 'payment_ids',
        type: 'array',
        required: false,
        description: `For bulk refunds: list of payment IDs to refund (max 10). Each gets the same amount. Use instead of payment_id.`,
      },
      {
        name: 'preview_token',
        type: 'string',
        required: false,
        description: `Required when confirmed=true: the preview_token field from this tool's own preceding preview response (the confirmed=false call for these exact same parameters). Rejected if missing, mismatched, already used, or if confirmed=true arrives too soon after the preview — a preview_token is only valid a few seconds after being issued, since it exists to prove a genuine user reply happened in between, not just that a preview call was made. If confirmed=true is rejected for being too soon, wait for the user's actual next message before trying again; do not immediately retry.`,
      },
      {
        name: 'reference',
        type: 'string',
        required: false,
        description: `Optional reference shown on the payer's bank statement where supported (max 18 chars for most schemes).`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_create_subscription',
    description: `Create a recurring subscription against a mandate, scheduling regular payments on a weekly, monthly, or yearly interval.`,
    params: [
      {
        name: 'amount',
        type: 'number',
        required: true,
        description: `Amount per payment in minor units (e.g. pence for GBP, cents for EUR).`,
      },
      {
        name: 'currency',
        type: 'string',
        required: true,
        description: `ISO 4217 currency code (e.g. GBP, EUR, USD). Must match the mandate's scheme.`,
      },
      {
        name: 'interval_unit',
        type: 'string',
        required: true,
        description: `Time unit between charges: weekly, monthly, or yearly.`,
      },
      {
        name: 'confirmed',
        type: 'boolean',
        required: false,
        description: `Set true only after the user has explicitly confirmed the preview in their most recent message. Default false (returns a preview).`,
      },
      {
        name: 'count',
        type: 'number',
        required: false,
        description: `Total number of payments to take. Omit (or set 0) for an indefinite subscription.`,
      },
      {
        name: 'day_of_month',
        type: 'number',
        required: false,
        description: `Day of month to charge (1–28, or -1 for last day). Required for monthly and yearly subscriptions.`,
      },
      {
        name: 'interval',
        type: 'number',
        required: false,
        description: `Number of interval_units between charges. Must be ≥ 1. Defaults to 1.`,
      },
      {
        name: 'mandate_id',
        type: 'string',
        required: false,
        description: `ID of the mandate to create the subscription against (e.g. MD000123).`,
      },
      {
        name: 'mandate_ids',
        type: 'array',
        required: false,
        description: `For bulk creation: list of mandate IDs to create the subscription against (max 25). Use instead of mandate_id.`,
      },
      {
        name: 'metadata',
        type: 'object',
        required: false,
        description: `Key-value store of custom data. Up to 3 keys, key names up to 50 chars, values up to 500 chars.`,
      },
      {
        name: 'month',
        type: 'string',
        required: false,
        description: `Month to charge, lowercase (e.g. january). Required for yearly subscriptions.`,
      },
      {
        name: 'name',
        type: 'string',
        required: false,
        description: `Name for the subscription (max 255 chars). Used as the description on each payment created.`,
      },
      {
        name: 'payment_reference',
        type: 'string',
        required: false,
        description: `Reference shown on the customer's bank statement. For Bacs: requires own Service User Number.`,
      },
      {
        name: 'preview_token',
        type: 'string',
        required: false,
        description: `Required when confirmed=true: the preview_token field from this tool's own preceding preview response (the confirmed=false call for these exact same parameters). Rejected if missing, mismatched, already used, or if confirmed=true arrives too soon after the preview — a preview_token is only valid a few seconds after being issued, since it exists to prove a genuine user reply happened in between, not just that a preview call was made. If confirmed=true is rejected for being too soon, wait for the user's actual next message before trying again; do not immediately retry.`,
      },
      {
        name: 'retry_if_possible',
        type: 'boolean',
        required: false,
        description: `Retry failed payments using intelligent retries (Success+). Defaults to false. Requires Success+ enabled in the GoCardless dashboard.`,
      },
      {
        name: 'start_date',
        type: 'string',
        required: false,
        description: `Date of the first payment (YYYY-MM-DD). Must be on or after the mandate's next_possible_charge_date. Defaults to next_possible_charge_date if omitted.`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_get_customer',
    description: `Retrieve a single customer by ID, with PII fields partially masked.`,
    params: [
      { name: 'id', type: 'string', required: true, description: `Customer ID (e.g. CU000123)` },
    ],
  },
  {
    name: 'gocardlessmcp_get_environment',
    description: `Return the current GoCardless environment (sandbox or live) and setup instructions.`,
    params: [],
  },
  {
    name: 'gocardlessmcp_get_mandate',
    description: `Retrieve a single mandate (Direct Debit authorisation) by its mandate ID.`,
    params: [
      { name: 'id', type: 'string', required: true, description: `Mandate ID (e.g. MD000123)` },
    ],
  },
  {
    name: 'gocardlessmcp_get_payment',
    description: `Retrieve a single payment by its payment ID.`,
    params: [
      { name: 'id', type: 'string', required: true, description: `Payment ID (e.g. PM000123)` },
    ],
  },
  {
    name: 'gocardlessmcp_get_payout',
    description: `Retrieve a single payout (bank settlement) by its payout ID.`,
    params: [
      { name: 'id', type: 'string', required: true, description: `Payout ID (e.g. PO000123)` },
    ],
  },
  {
    name: 'gocardlessmcp_get_refund',
    description: `Retrieve a single refund by its refund ID.`,
    params: [
      { name: 'id', type: 'string', required: true, description: `Refund ID (e.g. RF000123)` },
    ],
  },
  {
    name: 'gocardlessmcp_get_subscription',
    description: `Retrieve a single subscription (recurring payment schedule) by its subscription ID.`,
    params: [
      {
        name: 'id',
        type: 'string',
        required: true,
        description: `Subscription ID (e.g. SB000123)`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_integrate_with_gocardless',
    description: `Return an overview of GoCardless integration options for collecting one-off and recurring payments.`,
    params: [],
  },
  {
    name: 'gocardlessmcp_list_customers',
    description: `List customers, optionally filtered by creation date range.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page` },
      {
        name: 'created_at_gte',
        type: 'string',
        required: false,
        description: `Filter: created at or after this ISO 8601 datetime`,
      },
      {
        name: 'created_at_lte',
        type: 'string',
        required: false,
        description: `Filter: created at or before this ISO 8601 datetime`,
      },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      {
        name: 'sort_direction',
        type: 'string',
        required: false,
        description: `Sort direction: asc or desc`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_list_events',
    description: `List audit log events for state changes across all resources, optionally filtered by resource type, action, or date range.`,
    params: [
      {
        name: 'action',
        type: 'string',
        required: false,
        description: `Filter by action (e.g. created, confirmed, failed, paid_out, cancelled)`,
      },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page` },
      {
        name: 'created_at_gte',
        type: 'string',
        required: false,
        description: `Filter: created at or after this ISO 8601 datetime`,
      },
      {
        name: 'created_at_lte',
        type: 'string',
        required: false,
        description: `Filter: created at or before this ISO 8601 datetime`,
      },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      { name: 'mandate', type: 'string', required: false, description: `Filter by mandate ID` },
      { name: 'payment', type: 'string', required: false, description: `Filter by payment ID` },
      { name: 'payout', type: 'string', required: false, description: `Filter by payout ID` },
      { name: 'refund', type: 'string', required: false, description: `Filter by refund ID` },
      {
        name: 'resource_type',
        type: 'string',
        required: false,
        description: `Filter by resource type: payments, mandates, payouts, refunds, subscriptions, instalment_schedules`,
      },
      {
        name: 'subscription',
        type: 'string',
        required: false,
        description: `Filter by subscription ID`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_list_mandates',
    description: `List mandates (Direct Debit authorisations), optionally filtered by status, customer, or scheme.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page` },
      { name: 'customer', type: 'string', required: false, description: `Filter by customer ID` },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      {
        name: 'scheme',
        type: 'string',
        required: false,
        description: `Filter by scheme: bacs, sepa_core, ach, autogiro, becs, becs_nz, betalingsservice, faster_payments, pad, pay_to`,
      },
      {
        name: 'status',
        type: 'string',
        required: false,
        description: `Filter by status: pending_customer_approval, pending_submission, submitted, active, suspended_by_payer, failed, cancelled, expired, consumed, blocked`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_list_payments',
    description: `List payments, optionally filtered by status, customer, mandate, subscription, currency, or date range.`,
    params: [
      {
        name: 'after',
        type: 'string',
        required: false,
        description: `Cursor for next page (from previous response's next_cursor)`,
      },
      {
        name: 'created_at_gte',
        type: 'string',
        required: false,
        description: `Filter: created at or after this ISO 8601 datetime`,
      },
      {
        name: 'created_at_lte',
        type: 'string',
        required: false,
        description: `Filter: created at or before this ISO 8601 datetime`,
      },
      {
        name: 'currency',
        type: 'string',
        required: false,
        description: `Filter by currency code (e.g. GBP, EUR, USD)`,
      },
      {
        name: 'customer',
        type: 'string',
        required: false,
        description: `Filter by customer ID (e.g. CU000123)`,
      },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      {
        name: 'mandate',
        type: 'string',
        required: false,
        description: `Filter by mandate ID (e.g. MD000123)`,
      },
      {
        name: 'sort_direction',
        type: 'string',
        required: false,
        description: `Sort direction: asc or desc`,
      },
      {
        name: 'status',
        type: 'string',
        required: false,
        description: `Filter by status: pending_submission, submitted, confirmed, paid_out, cancelled, customer_approval_denied, failed, charged_back`,
      },
      {
        name: 'subscription',
        type: 'string',
        required: false,
        description: `Filter by subscription ID (e.g. SB000123)`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_list_payouts',
    description: `List payouts (bank settlements), optionally filtered by status, currency, or date range.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page` },
      {
        name: 'created_at_gte',
        type: 'string',
        required: false,
        description: `Filter: created at or after this ISO 8601 datetime`,
      },
      {
        name: 'created_at_lte',
        type: 'string',
        required: false,
        description: `Filter: created at or before this ISO 8601 datetime`,
      },
      {
        name: 'currency',
        type: 'string',
        required: false,
        description: `Filter by currency code (e.g. GBP, EUR, USD)`,
      },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      {
        name: 'status',
        type: 'string',
        required: false,
        description: `Filter by status: pending, paid, bounced`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_list_refunds',
    description: `List refunds, optionally filtered by payment, mandate, or date range.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page` },
      {
        name: 'created_at_gte',
        type: 'string',
        required: false,
        description: `Filter: created at or after this ISO 8601 datetime`,
      },
      {
        name: 'created_at_lte',
        type: 'string',
        required: false,
        description: `Filter: created at or before this ISO 8601 datetime`,
      },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      { name: 'mandate', type: 'string', required: false, description: `Filter by mandate ID` },
      { name: 'payment', type: 'string', required: false, description: `Filter by payment ID` },
    ],
  },
  {
    name: 'gocardlessmcp_list_subscriptions',
    description: `List subscriptions (recurring payment schedules), optionally filtered by status, customer, or mandate.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page` },
      { name: 'customer', type: 'string', required: false, description: `Filter by customer ID` },
      {
        name: 'limit',
        type: 'number',
        required: false,
        description: `Number of results per page (default 50, max 500)`,
      },
      { name: 'mandate', type: 'string', required: false, description: `Filter by mandate ID` },
      {
        name: 'status',
        type: 'string',
        required: false,
        description: `Filter by status: pending_customer_approval, active, cancelled, finished, paused`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_read_gocardless_resource',
    description: `Read the contents of a GoCardless resource by URI to fetch API endpoint details or documentation.`,
    params: [
      {
        name: 'uri',
        type: 'string',
        required: true,
        description: `The gocardless:// resource URI to read`,
      },
    ],
  },
  {
    name: 'gocardlessmcp_submit_feedback',
    description: `Submit a helpfulness rating (1–5) for the current MCP session, with an optional comment.`,
    params: [
      {
        name: 'rating',
        type: 'number',
        required: true,
        description: `How helpful was this session? 1 = not helpful at all, 5 = extremely helpful`,
      },
      {
        name: 'comment',
        type: 'string',
        required: false,
        description: `Optional additional feedback`,
      },
    ],
  },
]
