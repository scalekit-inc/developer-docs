import type { Tool } from '../../types/agent-connectors'

export const tools: Tool[] = [
  {
    name: 'kitmcp_add_subscriber_to_form',
    description: `Subscribe a single email address to a Kit form. Creates the subscriber if they do not exist; returns the subscriber record.`,
    params: [
      { name: 'email_address', type: 'string', required: true, description: `Subscriber's email address (required)` },
      { name: 'form_id', type: 'integer', required: true, description: `The form ID (required). Use list_forms to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_add_subscriber_to_sequence',
    description: `Enroll a single subscriber (by email) into a Kit email sequence. Use list_sequences to find the sequence ID.`,
    params: [
      { name: 'email_address', type: 'string', required: true, description: `Subscriber's email address (required)` },
      { name: 'sequence_id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_add_subscribers_to_form',
    description: `Subscribe multiple existing subscribers to one or more forms in a single request. Batches over 100 are processed asynchronously.`,
    params: [
      { name: 'additions', type: 'array', required: true, description: `Form subscriptions to create (required). Each item must include \`form_id\` and \`subscriber_id\`.` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_create_custom_fields',
    description: `Create multiple custom subscriber fields in one request. Use list_custom_fields to view existing fields.`,
    params: [
      { name: 'custom_fields', type: 'array', required: true, description: `Custom field definitions to create (required). Each item must include \`label\`.` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_create_subscribers',
    description: `Create or update multiple subscribers in one request. Batches over 100 are processed asynchronously.`,
    params: [
      { name: 'subscribers', type: 'array', required: true, description: `Subscribers to upsert (required). Each item must include \`email_address\`; \`first_name\` and \`state\` are optional.` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_create_tags',
    description: `Create multiple tags in one request. Returns created tag records.`,
    params: [
      { name: 'tags', type: 'array', required: true, description: `Tags to create (required). Each item must include \`name\`.` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_tag_subscribers',
    description: `Apply a tag to multiple subscribers in one request. Batches over 100 are processed asynchronously.`,
    params: [
      { name: 'taggings', type: 'array', required: true, description: `Tag-subscriber pairs to apply (required). Each item must include \`tag_id\` (integer) and \`subscriber_id\` (integer).` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_untag_subscribers',
    description: `Remove a tag from multiple subscribers in one request. Batches over 100 are processed asynchronously.`,
    params: [
      { name: 'taggings', type: 'array', required: true, description: `Tag-subscriber pairs to remove (required). Each item must include \`tag_id\` (integer) and \`subscriber_id\` (integer).` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_bulk_update_subscriber_custom_field_values',
    description: `Update custom field values for multiple subscribers in one request.`,
    params: [
      { name: 'custom_field_values', type: 'array', required: true, description: `Custom field updates to apply (required). Each item must include \`subscriber_id\`, \`subscriber_custom_field_id\`, and \`value\`.` },
      { name: 'callback_url', type: 'string', required: false, description: `URL to POST results to when the batch is processed asynchronously (batches > 100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_broadcast',
    description: `Create a draft email broadcast in Kit. The broadcast is saved as a draft; scheduling and sending happen from the Kit UI.`,
    params: [
      { name: 'content', type: 'string', required: true, description: `Email body content in HTML (required)` },
      { name: 'subject', type: 'string', required: true, description: `Email subject line (required)` },
      { name: 'allow_starting_point', type: 'boolean', required: false, description: `Set to true when posting adapted content from a Starting-point template (in combination with \`email_template_id\` set to that template's id and \`content\` set to the adapted body). Required only for the Starting-point + content combination; omit otherwise.` },
      { name: 'description', type: 'string', required: false, description: `Internal description for organizing broadcasts` },
      { name: 'email_template_id', type: 'integer', required: false, description: `Email template ID. Use list_email_templates to find IDs.` },
      { name: 'preview_text', type: 'string', required: false, description: `Preview text shown in email clients` },
      { name: 'public', type: 'boolean', required: false, description: `Whether to publish on the creator's profile page` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'subscriber_filter', type: 'array', required: false, description: `Target audience filter. A single-element array wrapping one filter group (all/any/none). Example: [{ "all": [{ "type": "tag", "ids": [1, 2] }] }]. Only one filter group (all, any, or none) may be populated per request. Filter types allowed: 'tag' or 'segment'. Omit to target all subscribers.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_custom_field',
    description: `Create a new custom subscriber field. Returns the created field record with its key.`,
    params: [
      { name: 'label', type: 'string', required: true, description: `Field label (required), e.g. 'Company' or 'Website'` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_sequence',
    description: `Create a new email sequence. Returns the sequence record including its ID.`,
    params: [
      { name: 'name', type: 'string', required: true, description: `Sequence name (required)` },
      { name: 'email_address', type: 'string', required: false, description: `From email address to use for this sequence` },
      { name: 'email_template_id', type: 'integer', required: false, description: `Email template ID. Use list_email_templates to find IDs.` },
      { name: 'repeat', type: 'boolean', required: false, description: `Whether the sequence repeats for subscribers` },
      { name: 'send_days', type: 'array', required: false, description: `Days to send on (e.g. ['monday', 'wednesday', 'friday'])` },
      { name: 'send_hour', type: 'integer', required: false, description: `Hour of day to send (0-23)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'time_zone', type: 'string', required: false, description: `Timezone for send scheduling (e.g. 'America/New_York')` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_sequence_email',
    description: `Add a new email to an existing sequence at a specified position and delay.`,
    params: [
      { name: 'delay_unit', type: 'string', required: true, description: `'days' or 'hours' (required)` },
      { name: 'delay_value', type: 'integer', required: true, description: `Number of days or hours to wait before sending this email (required). Unit is controlled by delay_unit.` },
      { name: 'sequence_id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'subject', type: 'string', required: true, description: `Email subject line (required)` },
      { name: 'content', type: 'string', required: false, description: `Email body content in HTML` },
      { name: 'email_template_id', type: 'integer', required: false, description: `Layout template ID. Use list_email_templates to find IDs.` },
      { name: 'position', type: 'integer', required: false, description: `0-based position in the sequence. Defaults to the end.` },
      { name: 'preview_text', type: 'string', required: false, description: `Preview text shown in email clients` },
      { name: 'published', type: 'boolean', required: false, description: `Publish the email immediately (default: false)` },
      { name: 'send_days', type: 'array', required: false, description: `Days of the week this email may send. Omit or pass null to allow all days (inherits the sequence schedule).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_snippet',
    description: `Create a reusable content snippet (inline or block) for use in broadcasts and sequences.`,
    params: [
      { name: 'name', type: 'string', required: true, description: `Snippet name (required). Used to auto-generate the key.` },
      { name: 'snippet_type', type: 'string', required: true, description: `'inline' or 'block' (required). Cannot be changed after creation.` },
      { name: 'content', type: 'string', required: false, description: `Text/Liquid content for inline snippets (required when snippet_type is 'inline')` },
      { name: 'document_attributes', type: 'object', required: false, description: `Rich HTML content for block snippets (required when snippet_type is 'block')` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_subscriber',
    description: `Create or update a single subscriber by email address (upsert). Returns the subscriber record.`,
    params: [
      { name: 'email_address', type: 'string', required: true, description: `Subscriber's email address (required)` },
      { name: 'fields', type: 'object', required: false, description: `Custom field values as key-value pairs` },
      { name: 'first_name', type: 'string', required: false, description: `Subscriber's first name` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'state', type: 'string', required: false, description: `Subscriber state (default: active)` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_tag',
    description: `Create a new tag. Returns the tag record with its ID.`,
    params: [
      { name: 'name', type: 'string', required: true, description: `Tag name (required)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_create_webhook',
    description: `Register a webhook endpoint to receive Kit events. Returns the created webhook record.`,
    params: [
      { name: 'event', type: 'object', required: true, description: `Event configuration (required). Must include 'name' and optional filter IDs.` },
      { name: 'target_url', type: 'string', required: true, description: `URL to receive webhook POST requests (required)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_delete_broadcast',
    description: `Delete a draft broadcast by ID. Only draft broadcasts can be deleted.`,
    params: [
      { name: 'broadcast_id', type: 'integer', required: true, description: `The broadcast ID to delete (required). Use list_broadcasts to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_delete_custom_field',
    description: `Delete a custom subscriber field by ID.`,
    params: [
      { name: 'custom_field_id', type: 'integer', required: true, description: `The custom field ID to delete (required). Use list_custom_fields to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_delete_sequence',
    description: `Delete a sequence and all its emails by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The sequence ID to delete (required). Use list_sequences to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_delete_sequence_email',
    description: `Delete a single email from a sequence by email ID and sequence ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The email ID to delete (required). Use list_sequence_emails to find IDs.` },
      { name: 'sequence_id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_delete_webhook',
    description: `Delete a registered webhook by ID.`,
    params: [
      { name: 'webhook_id', type: 'integer', required: true, description: `The webhook ID to delete (required). Use list_webhooks to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_filter_subscribers',
    description: `Search and filter subscribers by engagement events (opens, clicks, sends, deliveries) or sign-up date. Returns paginated results.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Pagination cursor for the next page. Use \`pagination.end_cursor\` from a previous response. Distinct from per-filter date ranges in \`all[].after\`.` },
      { name: 'all', type: 'array', required: false, description: `Array of filter conditions, AND-ed together. Each item filters by one event type (opens, clicks, sent, delivered) or by sign-up date (subscribed). Omit to match all subscribers.` },
      { name: 'before', type: 'string', required: false, description: `Pagination cursor for the previous page. Use \`pagination.start_cursor\` from a previous response. Distinct from per-filter date ranges in \`all[].before\`.` },
      { name: 'include_total_count', type: 'boolean', required: false, description: `If true, include \`pagination.total_count\` in the response (slower).` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100).` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'sort_field', type: 'string', required: false, description: `Field to sort by. Defaults to \`id\` (recommended for stable cursor pagination).` },
      { name: 'sort_order', type: 'string', required: false, description: `Sort direction (default: desc).` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_account',
    description: `Retrieve the Kit account details for the authenticated user.`,
    params: [
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_account_colors',
    description: `Retrieve the custom brand color palette for the Kit account.`,
    params: [
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_broadcast',
    description: `Retrieve a single broadcast record by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The broadcast ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_broadcast_clicks',
    description: `Retrieve click data for a specific broadcast, paginated by cursor.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The broadcast ID` },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_broadcast_stats',
    description: `Retrieve performance statistics (opens, clicks, etc.) for a specific broadcast.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The broadcast ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_broadcasts_stats',
    description: `Retrieve aggregated performance statistics for multiple broadcasts, with optional date and status filters.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'sent_after', type: 'string', required: false, description: `ISO 8601 date or datetime (e.g. \`2026-01-15\` or \`2026-01-15T10:00:00Z\`). Only include broadcasts with \`send_at >= sent_after\`.` },
      { name: 'sent_before', type: 'string', required: false, description: `ISO 8601 date or datetime (e.g. \`2026-01-15\` or \`2026-01-15T10:00:00Z\`). Only include broadcasts with \`send_at < sent_before\`.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'status', type: 'string', required: false, description: `Filter to broadcasts in this state. Omit to return all states.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_creator_profile',
    description: `Retrieve the creator profile linked to the authenticated Kit account.`,
    params: [
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_email_stats',
    description: `Retrieve overall email performance statistics for the Kit account.`,
    params: [
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_email_template',
    description: `Retrieve a single email template by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The email template ID (required). Use list_email_templates to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_growth_stats',
    description: `Retrieve subscriber growth statistics for a specified date range.`,
    params: [
      { name: 'ending', type: 'string', required: false, description: `End date (YYYY-MM-DD). Defaults to today.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'starting', type: 'string', required: false, description: `Start date (YYYY-MM-DD). Defaults to 90 days ago.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_post',
    description: `Retrieve a single Kit post (newsletter issue) by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The post ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_purchase',
    description: `Retrieve a single purchase record by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The purchase ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_sequence',
    description: `Retrieve a single sequence record by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The sequence ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_sequence_email',
    description: `Retrieve a single email within a sequence by email ID and sequence ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The email ID (required). Use list_sequence_emails to find IDs.` },
      { name: 'sequence_id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_snippet',
    description: `Retrieve a single content snippet by ID.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The snippet ID (required). Use list_snippets to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_subscriber',
    description: `Retrieve a single subscriber record by ID, including their custom fields.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The subscriber ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_subscriber_stats',
    description: `Retrieve engagement statistics for a single subscriber.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The subscriber ID` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_get_subscriber_tags',
    description: `Retrieve all tags applied to a specific subscriber, paginated.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The subscriber ID` },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_broadcasts',
    description: `List all broadcasts with optional status filter and cursor pagination.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include', type: 'array', required: false, description: `Extra fields to include per broadcast. \`content\` adds the full HTML body (can be large — only request when needed).` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'status', type: 'string', required: false, description: `Filter to broadcasts in this state. Omit to return all states.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_custom_fields',
    description: `List all custom subscriber fields in the account.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_email_templates',
    description: `List all email templates in the account.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_form_subscribers',
    description: `List all subscribers on a specific form, paginated.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The form ID. Use list_forms to find IDs.` },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include', type: 'array', required: false, description: `Extra data to include per subscriber. \`fields\` adds custom field values (omit for slim responses).` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_forms',
    description: `List all forms in the account with optional status filter.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'status', type: 'string', required: false, description: `Form status (default: active)` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_posts',
    description: `List all Kit newsletter posts with optional cursor pagination.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include_content', type: 'boolean', required: false, description: `Set to true to include the full HTML body of each post. Omit (or set false) for a slim response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_prompt_suggestions',
    description: `Retrieve suggested prompts to help the user get started with Kit via AI.`,
    params: [
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_purchases',
    description: `List all purchase records in the account, paginated.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_segments',
    description: `List all subscriber segments in the account.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_sequence_emails',
    description: `List all emails in a specific sequence.`,
    params: [
      { name: 'sequence_id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include_content', type: 'boolean', required: false, description: `Include HTML content in each email (default: false)` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_sequence_subscribers',
    description: `List all subscribers enrolled in a specific sequence.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The sequence ID` },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include', type: 'array', required: false, description: `Extra data to include per subscriber. \`fields\` adds custom field values (omit for slim responses).` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_sequences',
    description: `List all email sequences in the account.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_snippets',
    description: `List all content snippets in the account with optional type and archive filters.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'archived', type: 'boolean', required: false, description: `true to list archived snippets, false (default) for active ones` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include_content', type: 'boolean', required: false, description: `Include content and document fields in each snippet (default: false)` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'snippet_type', type: 'string', required: false, description: `Filter by type: 'inline' or 'block'` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_subscribers',
    description: `List all subscribers with optional status, sort, and cursor pagination.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include', type: 'array', required: false, description: `Extra data to include per subscriber. \`fields\` adds custom field values (omit for slim responses). \`canceled_at\` requires \`status\` set to \`cancelled\` (otherwise the API returns 422).` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'sort_field', type: 'string', required: false, description: `Sort by \`id\` (default, recommended for stable cursor pagination), \`created_at\`, or \`updated_at\`.` },
      { name: 'sort_order', type: 'string', required: false, description: `Sort direction: asc or desc` },
      { name: 'status', type: 'string', required: false, description: `Filter by subscriber state. Defaults to active when omitted. Use \`all\` to include every state.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_tag_subscribers',
    description: `List all subscribers who have a specific tag applied.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The tag ID. Use list_tags to find IDs.` },
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'include', type: 'array', required: false, description: `Extra data to include per subscriber. \`fields\` adds custom field values (omit for slim responses).` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_tags',
    description: `List all tags in the account.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_list_webhooks',
    description: `List all registered webhooks in the account.`,
    params: [
      { name: 'after', type: 'string', required: false, description: `Cursor for next page. Use \`pagination.end_cursor\` from a previous response.` },
      { name: 'before', type: 'string', required: false, description: `Cursor for previous page. Use \`pagination.start_cursor\` from a previous response.` },
      { name: 'per_page', type: 'integer', required: false, description: `Results per page (1-100)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_tag_subscriber',
    description: `Apply a tag to a subscriber identified by email address.`,
    params: [
      { name: 'email_address', type: 'string', required: true, description: `Subscriber's email address (required)` },
      { name: 'tag_id', type: 'integer', required: true, description: `The tag ID (required). Use list_tags to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_unsubscribe',
    description: `Cancel a subscriber's subscription by subscriber ID.`,
    params: [
      { name: 'subscriber_id', type: 'integer', required: true, description: `The subscriber ID (required). Use list_subscribers or get_subscriber to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_untag_subscriber',
    description: `Remove a tag from a subscriber by subscriber ID and tag ID.`,
    params: [
      { name: 'subscriber_id', type: 'integer', required: true, description: `The subscriber ID (required)` },
      { name: 'tag_id', type: 'integer', required: true, description: `The tag ID (required). Use list_tags to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_account_colors',
    description: `Update the custom brand color palette for the Kit account.`,
    params: [
      { name: 'colors', type: 'array', required: true, description: `Array of up to 10 hex color codes (e.g. ["#FF6900", "#FCB900"]). Replaces the existing palette entirely.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_broadcast',
    description: `Update a draft broadcast's subject, content, or audience filter.`,
    params: [
      { name: 'broadcast_id', type: 'integer', required: true, description: `The broadcast ID (required). Use list_broadcasts to find IDs.` },
      { name: 'content', type: 'string', required: false, description: `Email body content in HTML` },
      { name: 'description', type: 'string', required: false, description: `Internal description` },
      { name: 'email_template_id', type: 'integer', required: false, description: `Email template ID` },
      { name: 'preview_text', type: 'string', required: false, description: `Preview text shown in email clients` },
      { name: 'public', type: 'boolean', required: false, description: `Whether to publish on the creator's profile page` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'subject', type: 'string', required: false, description: `Email subject line` },
      { name: 'subscriber_filter', type: 'array', required: false, description: `Target audience filter. A single-element array wrapping one filter group (all/any/none). Example: [{ "all": [{ "type": "tag", "ids": [1, 2] }] }]. Only one filter group (all, any, or none) may be populated per request. Filter types allowed: 'tag' or 'segment'. Omit to leave current filter unchanged.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_custom_field',
    description: `Rename a custom subscriber field by ID.`,
    params: [
      { name: 'custom_field_id', type: 'integer', required: true, description: `The custom field ID (required). Use list_custom_fields to find IDs.` },
      { name: 'label', type: 'string', required: true, description: `New field label (required)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_sequence',
    description: `Update sequence settings such as name, send days, or active state.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'active', type: 'boolean', required: false, description: `Whether the sequence is active` },
      { name: 'email_address', type: 'string', required: false, description: `From email address` },
      { name: 'email_template_id', type: 'integer', required: false, description: `Email template ID` },
      { name: 'hold', type: 'boolean', required: false, description: `Whether the sequence is on hold` },
      { name: 'name', type: 'string', required: false, description: `New sequence name` },
      { name: 'repeat', type: 'boolean', required: false, description: `Whether the sequence repeats` },
      { name: 'send_days', type: 'array', required: false, description: `Days to send on` },
      { name: 'send_hour', type: 'integer', required: false, description: `Hour of day to send (0-23)` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'time_zone', type: 'string', required: false, description: `Timezone for send scheduling` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_sequence_email',
    description: `Update an existing sequence email's subject, content, delay, or position.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The email ID (required). Use list_sequence_emails to find IDs.` },
      { name: 'sequence_id', type: 'integer', required: true, description: `The sequence ID (required). Use list_sequences to find IDs.` },
      { name: 'content', type: 'string', required: false, description: `Email body content in HTML` },
      { name: 'delay_unit', type: 'string', required: false, description: `'days' or 'hours'` },
      { name: 'delay_value', type: 'integer', required: false, description: `Number of days or hours to wait before sending this email. Unit is controlled by delay_unit.` },
      { name: 'email_template_id', type: 'integer', required: false, description: `Layout template ID. Use list_email_templates to find IDs.` },
      { name: 'position', type: 'integer', required: false, description: `0-based position in the sequence` },
      { name: 'preview_text', type: 'string', required: false, description: `Preview text shown in email clients` },
      { name: 'published', type: 'boolean', required: false, description: `Publish or unpublish the email` },
      { name: 'send_days', type: 'array', required: false, description: `Days of the week this email may send. To reset to the full weekly schedule, pass all 7 values explicitly.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'subject', type: 'string', required: false, description: `Email subject line` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_snippet',
    description: `Update a content snippet's name, content, or archived state.`,
    params: [
      { name: 'id', type: 'integer', required: true, description: `The snippet ID (required). Use list_snippets to find IDs.` },
      { name: 'archived', type: 'boolean', required: false, description: `true to archive, false to restore` },
      { name: 'content', type: 'string', required: false, description: `New text/Liquid content (inline snippets only)` },
      { name: 'document_attributes', type: 'object', required: false, description: `New HTML content (block snippets only)` },
      { name: 'name', type: 'string', required: false, description: `New snippet name` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_subscriber',
    description: `Update a subscriber's email, name, or custom field values by subscriber ID.`,
    params: [
      { name: 'subscriber_id', type: 'integer', required: true, description: `The subscriber ID (required). Use list_subscribers or filter_subscribers to find IDs.` },
      { name: 'email_address', type: 'string', required: false, description: `New email address` },
      { name: 'fields', type: 'object', required: false, description: `Custom field values to update as key-value pairs` },
      { name: 'first_name', type: 'string', required: false, description: `New first name` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
  {
    name: 'kitmcp_update_tag',
    description: `Rename a tag by ID.`,
    params: [
      { name: 'name', type: 'string', required: true, description: `New tag name (required)` },
      { name: 'tag_id', type: 'integer', required: true, description: `The tag ID (required). Use list_tags to find IDs.` },
      { name: 'session_id', type: 'string', required: false, description: `UUID v4 identifier for grouping tool calls in the same conversation. Generate once at the start of the session and reuse it on every call.` },
      { name: 'user_goal', type: 'string', required: false, description: `What the creator is trying to accomplish with this tool call. Pick the closest match; use 'unknown' only when no category fits.` },
    ],
  },
]
