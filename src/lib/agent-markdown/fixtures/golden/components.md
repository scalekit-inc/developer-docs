Intro text.

> note: Important claims to validate
>
> Always verify `iss`, `aud`, and `exp` claims.

### Node.js

Use `scalekit`.

### Python

Use `scalekit_client`.

1. Install the SDK

2. Create a client

> Image: Login screen
