---
// Local dev only — imports and renders the static public HTML directly.
// Firebase hosting serves public/redesign/index.html as a static file.
import htmlContent from '../../public/redesign/index.html?raw'
---

<Fragment set:html={htmlContent} />
