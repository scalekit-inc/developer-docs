---
// Redirect /sandbox/* to /code-samples/samples/*
export const prerender = false

const { slug } = Astro.params
const redirectUrl = `/code-samples/samples/${slug || ''}`

return Astro.redirect(redirectUrl, 301)
---
