---
// Redirect /sandbox/* to /dev-kit/code-samples/samples/*
export const prerender = false

const { slug } = Astro.params
const redirectUrl = `/dev-kit/code-samples/samples/${slug || ''}`

return Astro.redirect(redirectUrl, 301)
---
