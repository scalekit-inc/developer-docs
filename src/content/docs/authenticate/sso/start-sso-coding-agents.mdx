---
title: 'Coding agents: Add SSO to your app'
description: 'Let your coding agents guide you into adding Scalekit SSO to your existing application in minutes'
tags: [modular-sso, quickstart]
sidebar:
  label: 'Start with coding agents'
  order: 2
tableOfContents: false
head:
  - tag: style
    content: |
      .sl-markdown-content h2 {
        font-size: var(--sl-text-xl);
      }
      .sl-markdown-content h3 {
        font-size: var(--sl-text-lg);
      }
next:
  label: 'Modular SSO implementation guide'
  link: '/authenticate/sso/add-modular-sso/'
seeAlso:
  label: 'See also'
  expanded: true
  items:
    - title: 'Browse SSO code samples'
      icon: 'github'
      url: 'https://github.com/scalekit-inc/scalekit-express-example'
---

import { Steps, Aside, Tabs, TabItem } from '@astrojs/starlight/components'

Use AI coding agents to accelerate your Modular SSO integration. This guide shows you how to configure and use coding agents like Claude Code to implement Scalekit's SSO end-to-end in your existing applications.

AI-assisted development reduces implementation time from hours to minutes. The coding agent analyzes your codebase, applies SSO patterns, and generates production-ready code that follows security best practices for enterprise identity provider integration.

<Steps>

1. ## Install your coding agent

   Install an AI coding agent that supports plugin ecosystems. Claude Code provides native integration with Scalekit's authentication plugins.

   <Tabs>
   <TabItem value="claude" label="Claude Code">

   Run the installation script to set up Claude Code on your system:

   > **Security Note**: Before piping to bash, verify the installer checksum or signature using the provider's verification method, or use a signed/package-manager alternative. See the [official quickstart guide](https://code.claude.com/docs/en/quickstart) for secure installation options.

   ```bash title="Terminal" frame="terminal" showLineNumbers=false
   curl -fsSL https://claude.ai/install.sh | bash
   ```

   After installation completes, verify setup by running:

   ```bash title="Terminal" frame="terminal" showLineNumbers=false
   claude --version
   ```

   Learn more about Claude Code in the [official quickstart guide](https://code.claude.com/docs/en/quickstart).

   </TabItem>
   </Tabs>

   <Aside type="tip" title="Other AI coding tools">
   While this guide focuses on Claude Code, the same authentication patterns apply to other AI-assisted development tools. The implementation principles remain consistent regardless of your chosen coding agent.
   </Aside>

2. ## Add the Scalekit Auth Stack marketplace

   Register Scalekit's plugin marketplace to access pre-configured authentication skills. This marketplace provides context-aware prompts and implementation guides that help coding agents generate correct Modular SSO code.

   <Tabs>
   <TabItem value="claude" label="Claude Code">

   Start the Claude Code REPL and add the marketplace:

   ```bash title="Terminal" frame="terminal" showLineNumbers=false
   # Start Claude REPL
   claude

   # Add Scalekit Auth Stack marketplace
   /plugin marketplace add scalekit-inc/claude-code-authstack
   ```

   When the marketplace registers successfully, you'll see confirmation output:

   ```bash title="Terminal" frame="terminal" showLineNumbers=false
   ❯ /plugin marketplace add scalekit-inc/claude-code-authstack
      ⎿  Successfully added marketplace: scalekit-auth-stack
   ```

   </TabItem>
   </Tabs>

   The marketplace provides specialized authentication plugins that understand SSO patterns and SAML/OIDC security requirements. These plugins guide the coding agent to generate implementation code that matches your project structure.

   To remove the Scalekit Auth Stack marketplace, use the uninstall command:

   <Tabs>
   <TabItem value="claude" label="Claude Code">

   ```bash title="Claude REPL" showLineNumbers=false
   /plugin marketplace remove scalekit-auth-stack
   ```

   </TabItem>
   </Tabs>

3. ## Enable authentication plugins

   Select which authentication capabilities to activate in your development environment. Each plugin provides specific skills that the coding agent uses to generate SSO code.

   Run the plugin wizard to browse and enable available plugins:

   <Tabs>
   <TabItem value="claude" label="Claude Code">

   ```bash title="Claude REPL" showLineNumbers=false
   /plugins
   ```

   </TabItem>
   </Tabs>

   Navigate through the visual interface to enable the Modular SSO plugin.

   <Aside type="tip" title="Auto-update recommendations">
   Enable auto-updates for authentication plugins to receive security patches and improvements. Scalekit regularly updates plugins based on community feedback and security best practices.
   </Aside>

   Or, directly install the specific plugin:

   <Tabs>
   <TabItem value="claude" label="Claude Code">

   ```bash title="Claude REPL" showLineNumbers=false
   /plugin install modular-sso@scalekit-auth-stack
   ```

   </TabItem>
   </Tabs>

   To remove an installed plugin, use the uninstall command:

   <Tabs>
   <TabItem value="claude" label="Claude Code">

   ```bash title="Claude REPL" showLineNumbers=false
   /plugin uninstall modular-sso@scalekit-auth-stack
   ```

   </TabItem>
   </Tabs>

4. ## Generate SSO implementation

   Use a structured prompt to direct the coding agent. A well-formed prompt ensures the agent generates complete, production-ready SSO code that includes all required security components.

   Copy the following prompt into your coding agent:

   ```md wrap showLineNumbers=false title="SSO implementation prompt"
   Guide the coding agent to add Scalekit SSO to my existing app — initialize ScalekitClient, generate an SSO authorization URL for a given organization, handle the SSO callback to validate and exchange the code for user identity, and integrate the SSO user into my existing session system. Code only.
   ```

   When you submit this prompt, Claude Code:
   1. Loads the Modular SSO skill from the marketplace
   2. Analyzes your existing application structure
   3. Generates Scalekit client initialization with environment credentials
   4. Creates an SSO authorization URL generator for organization-based routing
   5. Implements the SSO callback handler to validate and exchange the code for user identity
   6. Integrates SSO user data into your existing session system

   <Aside type="caution" title="Review generated code">
   Always review AI-generated authentication code before deployment. Verify that environment variables, token validation logic, and error handling match your security requirements. The coding agent provides a foundation, but you must ensure it aligns with your application's specific needs.
   </Aside>

5. ## Verify the implementation

   After the coding agent completes, verify that all SSO components are properly configured:

   Check generated files:
   - Scalekit client initialization with environment credentials (you may need to set up a `.env` file with your Scalekit API credentials)
   - SSO authorization URL generation for organization-based routing
   - SSO callback handler that validates the authorization code and retrieves user identity
   - Integration logic that maps SSO user identity into your existing session system

   The SSO flow should redirect users to their organization's identity provider, where they authenticate. Your application should then receive the callback, validate the code, extract the user's identity, and create or update the user session accordingly.

</Steps>

When users authenticate through SSO, your application receives verified identity claims from the identity provider. Verify that the SSO callback correctly maps user identity to your application's user model and that the session is created with the appropriate access level.

<Aside type="note" title="Beta program feedback">
Scalekit's Auth Stack marketplace is in public beta. Join the [Slack community](https://join.slack.com/t/scalekit-community/shared_invite/zt-3gsxwr4hc-0tvhwT2b_qgVSIZQBQCWRw) to share feedback and connect with other developers. Scalekit is actively expanding support beyond Claude Code—tell us which AI tools you use.
</Aside>
