---
title: Secure MCP with Microsoft Login
description: "Learn how to integrate Microsoft Sign-In with Scalekit, enabling secure social authentication for your users with step-by-step OAuth configuration instructions."
sidebar:
  order: 2
  label: "Microsoft"
tableOfContents:
  maxHeadingLevel: 3
head:
  - tag: style
    content: |
      table td:not(:last-child), table th:not(:last-child) {
        white-space: nowrap;
      }
      .sl-markdown-content h2 {
          font-size: var(--sl-text-xl);
       }
      .sl-markdown-content h3 {
          font-size: var(--sl-text-lg);
       }
prev:
  label: "Google"
  link: /guides/integrations/social-connections/google
next:
  label: "GitHub"
  link: /guides/integrations/social-connections/github
---

import { getEntry, render } from 'astro:content'

export default async function Page(props) {
  const guide = await getEntry('docs', 'guides/integrations/social-connections/microsoft')
  const { Content } = await render(guide)

  if (import.meta.env.DEV) {
    const fieldsToCheck = ['title', 'description', 'sidebar', 'tableOfContents', 'prev', 'next']
    const mismatches = fieldsToCheck.filter((field) => {
      const ourValue = structuredClone(frontmatter[field])
      const theirValue = structuredClone(guide.data[field])
      return JSON.stringify(ourValue) !== JSON.stringify(theirValue)
    })
    if (mismatches.length) {
      console.warn(
        `mcp/social-connections/microsoft frontmatter differs from guides/integrations/social-connections/microsoft: ${mismatches.join(', ')}`,
      )
    }
  }

  return (
    <>
      <Content {...props} />
    </>
  )
}
