---
title: Scalekit Docs
template: splash
description: Auth, provider connections, and tool execution for AI agents and SaaS apps
tableOfContents: false
prev: false
next: false
head:
  - tag: script
    content: |
      (function () {
        try {
          var path = localStorage.getItem('sk_docs_path');
          if (path === 'agent-actions') { window.location.replace('/home/agent-actions/'); return; }
          if (path === 'auth-for-saas') { window.location.replace('/home/auth-for-saas/'); return; }
        } catch (e) {}
      })();
  - tag: style
    content: |
      body:has(.gateway) right-sidebar-panel { display: none !important; }
      body:has(.gateway) .hero { display: none !important; }
      body:has(.gateway) .meta.sl-flex { display: none !important; }
      body:has(.gateway) footer.sl-flex { display: none !important; }
      body:has(.gateway) .secondary-nav { display: none !important; }
      body:has(.gateway) .page-title-wrapper { display: none !important; }
      body:has(.gateway) .content-panel:has(.page-title-wrapper) {
        display: none !important;
      }
      body:has(.gateway) .feedback-section {
        display: none !important;
        margin-top: 0 !important;
      }
      body:has(.gateway) button[aria-label="Menu"].sl-flex.md\:sl-hidden {
        display: none !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) {
        padding-top: 0 !important;
        border-top: 0 !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) > .sl-container {
        max-width: none !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) > .sl-container > .sl-markdown-content {
        margin-top: 0 !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) starlight-image-zoom {
        display: none !important;
      }

      .gateway-shell {
        width: 100vw;
        margin-left: 50%;
        transform: translateX(-50%);
        position: relative;
        z-index: 1;
      }

      .gateway-intro {
        background: #fff;
        border-bottom: 1px solid var(--sl-color-hairline);
      }

      .gateway-intro-inner {
        max-width: 1280px;
        margin: 0 auto;
        padding: 3.5rem 4rem 3rem;
      }

      .gateway-intro-kicker {
        margin: 0 0 0.75rem;
        font-size: 0.8rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--sl-color-text-muted);
      }

      .gateway-intro-title {
        max-width: 42rem;
        margin: 0;
        font-size: clamp(2rem, 1.6rem + 1.2vw, 3rem);
        line-height: 1.08;
        letter-spacing: -0.04em;
        color: var(--sl-color-text-heading);
      }

      .gateway-intro-copy {
        max-width: 46rem;
        margin: 1rem 0 0;
        font-size: 1.05rem;
        line-height: 1.7;
        color: var(--sl-color-text);
      }

      .gateway {
        display: flex;
        flex-direction: row;
        min-height: clamp(30rem, calc(100vh - var(--sl-nav-height) - 20rem), 42rem);
        margin-top: 0;
        overflow: hidden;
        background: var(--sl-color-bg);
      }

      .panel {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 1.25rem;
        padding: 3.5rem 3rem;
        text-align: center;
        text-decoration: none;
        color: inherit;
        transition:
          flex 0.4s cubic-bezier(0.4, 0, 0.2, 1),
          opacity 0.25s ease,
          background 0.2s ease,
          box-shadow 0.2s ease;
        position: relative;
        overflow: hidden;
      }

      .panel-agents {
        background: linear-gradient(180deg, var(--sl-color-bg-card) 0%, var(--sl-color-bg-nav) 100%);
      }

      .panel-saas {
        background: linear-gradient(180deg, var(--sl-color-bg-card) 0%, var(--sl-color-gray-6) 100%);
      }

      .panel::before {
        content: '';
        position: absolute;
        inset: 0;
        pointer-events: none;
        background:
          radial-gradient(circle at top center, color-mix(in srgb, var(--sl-color-accent) 14%, transparent) 0%, transparent 58%);
        opacity: 0.7;
      }

      @media (hover: hover) and (min-width: 1024px) {
        .gateway:hover .panel:not(:hover) {
          flex: 0.6;
          opacity: 0.72;
        }
        .gateway:hover .panel:hover {
          flex: 1.4;
          box-shadow: inset 0 0 0 1px var(--sl-color-hairline);
        }
        .panel-agents:hover {
          justify-content: flex-start;
          background: linear-gradient(180deg, var(--sl-color-bg-card) 0%, color-mix(in srgb, var(--sl-color-bg-nav) 88%, var(--sl-color-accent-low)) 100%);
        }
        .panel-saas:hover {
          background: linear-gradient(180deg, var(--sl-color-bg-card) 0%, color-mix(in srgb, var(--sl-color-gray-6) 92%, var(--sl-color-bg-nav)) 100%);
        }
      }

      .panel-divider {
        width: 1px;
        background: var(--sl-color-hairline);
        flex-shrink: 0;
        align-self: stretch;
      }

      .panel-badge {
        font-size: 0.75rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        font-weight: 600;
        color: var(--sl-color-text-muted);
        margin-bottom: 0;
      }

      .panel-icon-shell {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 4.25rem;
        height: 4.25rem;
        border-radius: 999px;
        border: 1px solid var(--sl-color-hairline);
        background: color-mix(in srgb, var(--sl-color-bg-card) 84%, transparent);
        box-shadow: var(--sl-shadow-sm);
      }

      .panel-icon {
        width: 2rem;
        height: 2rem;
        color: var(--sl-color-accent);
      }

      .panel-label {
        font-size: clamp(1.625rem, 1.2rem + 1vw, 2.2rem);
        font-weight: 600;
        color: var(--sl-color-text-heading);
        line-height: 1.15;
        letter-spacing: -0.03em;
        max-width: 23rem;
        margin: 0;
      }

      .panel-desc {
        font-size: 1rem;
        color: var(--sl-color-text-muted);
        line-height: 1.6;
        max-width: 24rem;
        margin: 0;
      }

      .panel-visual {
        width: min(100%, 27rem);
        margin: 0.1rem auto 0;
        border-radius: 1.15rem;
        border: 1px solid var(--sl-color-hairline);
        background: color-mix(in srgb, var(--sl-color-bg-card) 94%, white);
        box-shadow: var(--sl-shadow-sm);
        overflow: hidden;
        text-align: left;
      }

      .panel-visual-header {
        padding: 0.8rem 0.95rem;
        border-bottom: 1px solid var(--sl-color-hairline);
        background: color-mix(in srgb, var(--sl-color-accent-low) 22%, white);
      }

      .panel-visual-title {
        display: block;
        font-size: 0.92rem;
        font-weight: 600;
        line-height: 1.3;
        color: var(--sl-color-text-heading);
      }

      .panel-visual-subtitle {
        display: block;
        margin-top: 0.2rem;
        font-size: 0.76rem;
        line-height: 1.45;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-body {
        display: grid;
        gap: 0.7rem;
        padding: 0.95rem;
      }

      .panel-visual-node {
        border-radius: 0.9rem;
        border: 1px solid var(--sl-color-hairline);
        background: var(--sl-color-bg-card);
        padding: 0.8rem 0.9rem;
      }

      .panel-visual-label {
        display: block;
        margin-bottom: 0.25rem;
        font-size: 0.68rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-node strong {
        display: block;
        font-size: 0.92rem;
        line-height: 1.3;
        color: var(--sl-color-text-heading);
      }

      .panel-visual-node span {
        display: block;
        margin-top: 0.25rem;
        font-size: 0.78rem;
        line-height: 1.45;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-arrow {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
        font-weight: 700;
        line-height: 1;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-list {
        list-style: none;
        margin: 0;
        padding: 0;
        border-radius: 0.9rem;
        border: 1px solid var(--sl-color-hairline);
        background: var(--sl-color-bg-card);
        overflow: hidden;
      }

      .panel-visual-list li {
        padding: 0.7rem 0.9rem;
        border-top: 1px solid var(--sl-color-hairline);
      }

      .panel-visual-list li:first-child {
        border-top: 0;
      }

      .panel-visual-list strong {
        display: block;
        font-size: 0.82rem;
        line-height: 1.3;
        color: var(--sl-color-text-heading);
      }

      .panel-visual-list span {
        display: block;
        margin-top: 0.2rem;
        font-size: 0.75rem;
        line-height: 1.45;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-chip-row {
        display: flex;
        flex-wrap: wrap;
        gap: 0.4rem;
      }

      .panel-visual-chip {
        display: inline-flex;
        align-items: center;
        padding: 0.26rem 0.56rem;
        border-radius: 999px;
        border: 1px solid var(--sl-color-hairline);
        background: color-mix(in srgb, var(--sl-color-bg-card) 90%, white);
        font-size: 0.72rem;
        font-weight: 500;
        color: var(--sl-color-text-muted);
        white-space: nowrap;
      }

      .panel-visual-saas {
        width: min(100%, 31rem);
      }

      .panel-visual-saas .panel-visual-header {
        background: color-mix(in srgb, var(--sl-color-accent-low) 28%, white);
      }

      .panel-visual-saas-body {
        padding: 0.95rem;
      }

      .saas-arch {
        display: block;
      }

      .panel-cta {
        font-size: 0.95rem;
        font-weight: 600;
        margin-top: 0.25rem;
        color: var(--sl-color-accent);
      }

      @media (max-width: 1023px) and (min-width: 768px) {
        .gateway-intro-inner {
          padding: 3rem 2rem 2.5rem;
        }
        .panel {
          padding: 3rem 2rem;
        }
        .panel-label {
          max-width: 18rem;
        }
        .panel-desc {
          font-size: 0.95rem;
          max-width: 18rem;
        }
        .panel-visual {
          width: min(100%, 22rem);
        }
        .panel-visual-saas {
          width: min(100%, 25rem);
        }
      }

      @media (max-width: 767px) {
        .gateway-intro-inner {
          padding: 2.5rem 1rem 2rem;
        }
        .gateway-intro-copy {
          font-size: 1rem;
          line-height: 1.65;
        }
        .gateway {
          flex-direction: column;
          min-height: auto;
        }
        .panel-divider {
          width: 100%;
          height: 1px;
          align-self: auto;
        }
        .panel {
          flex: 1;
          padding: 2rem 1.5rem;
        }
        .panel-label {
          max-width: 100%;
        }
        .panel-desc {
          max-width: 100%;
        }
        .panel-visual {
          width: 100%;
        }
      }

      @media (hover: hover) and (min-width: 1024px) {
        .panel-agents .panel-visual {
          max-height: 0;
          opacity: 0;
          transform: translateY(0.75rem);
          margin-top: 0;
          border-width: 0;
          box-shadow: none;
          transition:
            max-height 0.35s ease,
            opacity 0.2s ease,
            transform 0.25s ease,
            margin-top 0.25s ease,
            border-width 0.2s ease;
        }

        .panel-agents:hover .panel-visual {
          max-height: 48rem;
          opacity: 1;
          transform: translateY(0);
          margin-top: 0.1rem;
          border-width: 1px;
          box-shadow: var(--sl-shadow-sm);
        }

        .panel-saas .panel-visual {
          max-height: 0;
          opacity: 0;
          transform: translateY(0.75rem);
          margin-top: 0;
          border-width: 0;
          box-shadow: none;
          transition:
            max-height 0.35s ease,
            opacity 0.2s ease,
            transform 0.25s ease,
            margin-top 0.25s ease,
            border-width 0.2s ease;
        }

        .panel-saas:hover {
          justify-content: flex-start;
        }

        .panel-saas:hover .panel-visual {
          max-height: 48rem;
          opacity: 1;
          transform: translateY(0);
          margin-top: 0.1rem;
          border-width: 1px;
          box-shadow: var(--sl-shadow-sm);
        }
      }
---

import IconLucideBot from '~icons/lucide/bot'
import IconLucideShield from '~icons/lucide/shield'
import IconLucideBotMessageSquare from '~icons/lucide/bot-message-square'
import IconLucideLaptop from '~icons/lucide/laptop'

<h1 class="sr-only">Choose your Scalekit documentation path</h1>

<div class="gateway-shell">
  <section class="gateway-intro" aria-labelledby="gateway-intro-title">
    <div class="gateway-intro-inner">
      <p class="gateway-intro-kicker">Welcome to Scalekit Docs</p>
      <h2 class="gateway-intro-title" id="gateway-intro-title">
        Choose the fastest path to the auth stack you need
      </h2>
      <p class="gateway-intro-copy">
        Scalekit docs help you ship authentication, provider connections, and secure tool access without rebuilding the
        entire identity layer yourself.
      </p>
      <p class="gateway-intro-copy">
        Start with the guide that matches your product. Pick <strong>Agent Auth</strong> if you are building AI agents
        that act for users, or pick <strong>SaaS Auth</strong> if you need SSO, SCIM, and user management for a B2B
        application.
      </p>
    </div>
  </section>

  <div class="gateway">
    <a
      href="/home/agent-actions/"
      class="panel panel-agents"
      onclick="try{localStorage.setItem('sk_docs_path','agent-actions')}catch(e){}"
    >
      <span class="panel-badge">For AI developers</span>
      <span class="panel-icon-shell" aria-hidden="true">
        <IconLucideBot class="panel-icon" />
      </span>
      <p class="panel-label">Build agents that act on behalf of your users</p>
      <div class="panel-visual" aria-hidden="true">
        <div class="panel-visual-header">
          <span class="panel-visual-title">Agent calls move through Scalekit</span>
          <span class="panel-visual-subtitle">Delegated identity, scoped access, and user-authorized tool execution.</span>
        </div>
        <div class="panel-visual-body">
          <div class="panel-visual-node">
            <span class="panel-visual-label">Start</span>
            <strong>Your agent</strong>
            <span>Codex, Claude, Cursor, or your own runtime.</span>
          </div>

          <div class="panel-visual-arrow">↓</div>

          <ul class="panel-visual-list">
            <li>
              <strong>Delegated auth</strong>
              <span>Exchange and store user credentials safely.</span>
            </li>
            <li>
              <strong>Scoped permissions</strong>
              <span>Limit actions by user, organization, and tool.</span>
            </li>
            <li>
              <strong>Tool execution</strong>
              <span>Run connected actions on behalf of the signed-in user.</span>
            </li>
          </ul>

          <div class="panel-visual-arrow">↓</div>

          <div class="panel-visual-node">
            <span class="panel-visual-label">Connect</span>
            <strong>User tools</strong>
            <div class="panel-visual-chip-row">
              <span class="panel-visual-chip">Slack</span>
              <span class="panel-visual-chip">HubSpot</span>
              <span class="panel-visual-chip">MCP</span>
              <span class="panel-visual-chip">Databases</span>
            </div>
          </div>
        </div>
      </div>
      <span class="panel-cta">Start with Agent Auth →</span>
    </a>

    <div class="panel-divider" role="separator"></div>

    <a
      href="/home/auth-for-saas/"
      class="panel panel-saas"
      onclick="try{localStorage.setItem('sk_docs_path','auth-for-saas')}catch(e){}"
    >
      <span class="panel-badge">For SaaS developers</span>
      <span class="panel-icon-shell" aria-hidden="true">
        <IconLucideShield class="panel-icon" />
      </span>
      <p class="panel-label">Add auth to your B2B SaaS</p>
      <div class="panel-visual panel-visual-saas" aria-hidden="true">
        <div class="panel-visual-header">
          <span class="panel-visual-title">Auth architecture across your API app</span>
          <span class="panel-visual-subtitle">Inbound auth, your API app, and outbound user-authorized connections.</span>
        </div>
        <div class="panel-visual-saas-body">
          <div class="saas-arch">
            <img
              src="https://cdn.prod.website-files.com/65b87d98fa638289e10b8f61/695c99460b2166ea712fe179_flow-img-D.avif"
              alt="Authentication architecture overview"
              style="width: 100%; height: auto; border-radius: 1rem;"
            />
          </div>
        </div>
      </div>
      <span class="panel-cta">Start with SaaS Auth →</span>
    </a>
  </div>
</div>
