---
title: Scalekit Docs
template: splash
description: Auth, provider connections, and tool execution for AI agents and SaaS apps
tableOfContents: false
prev: false
next: false
head:
  - tag: style
    content: |
      body:has(.gateway) right-sidebar-panel { display: none !important; }
      body:has(.gateway) .hero { display: none !important; }
      body:has(.gateway) .meta.sl-flex { display: none !important; }
      body:has(.gateway) footer.sl-flex { display: none !important; }
      body:has(.gateway) .secondary-nav { display: none !important; }
      body:has(.gateway) .page-title-wrapper { display: none !important; }
      body:has(.gateway) .content-panel:has(.page-title-wrapper) {
        display: none !important;
      }
      body:has(.gateway) .feedback-section {
        display: none !important;
        margin-top: 0 !important;
      }
      body:has(.gateway) button[aria-label="Menu"].sl-flex.md\:sl-hidden {
        display: none !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) {
        padding-top: 0 !important;
        border-top: 0 !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) > .sl-container {
        max-width: none !important;
      }
      body:has(.gateway) .content-panel:has(.gateway-shell) > .sl-container > .sl-markdown-content {
        margin-top: 0 !important;
      }
      body:has(.gateway) starlight-image-zoom-zoomable {
        display: contents !important;
        pointer-events: none !important;
      }
      body:has(.gateway) starlight-image-zoom-zoomable button {
        display: none !important;
      }

      body:has(.gateway) .gateway-shell img,
      body:has(.gateway) .gateway-shell picture {
        border: none !important;
        outline: none !important;
        box-shadow: none !important;
      }

      body:has(.gateway) .gateway-shell starlight-image-zoom-zoomable {
        border: none !important;
        box-shadow: none !important;
      }

      .gateway-shell {
        width: 100vw;
        margin-left: 50%;
        transform: translateX(-50%);
        position: relative;
        z-index: 1;
        display: flex;
        flex-direction: column;
        min-height: calc(100dvh - var(--sl-nav-height, 6.3rem));
      }

      .gateway-section-label {
        font-size: 0.8125rem;
        font-weight: 600;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        color: var(--sl-color-text-muted);
        text-align: center;
        margin: 0 0 1rem;
        padding: 0;
      }

      .gateway { order: -1; }
      .gateway-cli { order: 1; }

      .gateway {
        display: flex;
        flex-direction: row;
        flex: 1;
        min-height: 0;
        margin-top: 0;
        padding-top: 0;
        overflow: hidden;
        background: var(--sl-color-bg);
      }

      .panel {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 1rem;
        padding: 2rem 2.5rem;
        text-align: center;
        text-decoration: none;
        color: inherit;
        transition:
          flex 0.45s cubic-bezier(0.4, 0, 0.2, 1),
          opacity 0.3s ease,
          background 0.3s ease;
        position: relative;
        overflow: hidden;
        border-radius: 0;
      }

      .panel:focus-visible {
        outline: var(--sk-focus-ring, 2px solid var(--sl-color-accent));
        outline-offset: -2px;
        z-index: 1;
      }

      @media (prefers-reduced-motion: reduce) {
        .panel {
          transition: none;
        }
      }

      /* Soft paper gradients — theme-aware via --sk-color-agent-* tokens */
      .panel-agents {
        background: linear-gradient(
          180deg,
          var(--sl-color-bg) 0%,
          color-mix(in srgb, var(--sk-color-agent-soft, #ecfdf5) 70%, var(--sl-color-bg)) 100%
        );
      }

      .panel-saas {
        background: linear-gradient(
          180deg,
          var(--sl-color-bg) 0%,
          color-mix(in srgb, var(--sl-color-gray-6) 55%, var(--sl-color-bg)) 100%
        );
      }

      :root[data-theme='dark'] .panel-agents {
        background: linear-gradient(
          180deg,
          var(--sl-color-bg) 0%,
          color-mix(in srgb, var(--sk-color-agent-deep, #052e16) 45%, var(--sl-color-bg)) 100%
        );
      }

      :root[data-theme='dark'] .panel-saas {
        background: linear-gradient(
          180deg,
          var(--sl-color-bg) 0%,
          color-mix(in srgb, var(--sl-color-gray-6) 40%, var(--sl-color-bg)) 100%
        );
      }

      .panel::before {
        content: '';
        position: absolute;
        inset: 0;
        pointer-events: none;
        background:
          radial-gradient(circle at top center, color-mix(in srgb, var(--sl-color-accent) 14%, transparent) 0%, transparent 58%);
        opacity: 0.7;
      }

      .panel-agents .panel-badge {
        background-color: color-mix(
          in srgb,
          var(--sk-color-agent-soft, #ecfdf5) 80%,
          var(--sl-color-bg-card)
        );
        border-color: color-mix(
          in srgb,
          var(--sk-color-agent-mid, #16b364) 55%,
          var(--sl-color-hairline)
        );
        color: color-mix(in srgb, var(--sk-color-agent-ink, #0e8a4a) 85%, var(--sl-color-text));
      }

      .panel-saas .panel-badge {
        background-color: var(--sl-color-gray-6);
        border-color: var(--sl-color-hairline);
        color: var(--sl-color-text-muted);
      }

      :root[data-theme='dark'] .panel-agents .panel-badge {
        background-color: color-mix(
          in srgb,
          var(--sk-color-agent-deep, #052e16) 55%,
          var(--sl-color-bg-card)
        );
        border-color: color-mix(
          in srgb,
          var(--sk-color-agent-mid, #16b364) 40%,
          var(--sl-color-hairline)
        );
        color: var(--sk-color-agent-ink, var(--sl-color-green-high, #6ee7b7));
      }

      @media (hover: hover) and (min-width: 1024px) {
        /* Don't dim a keyboard-focused panel when the other is hovered */
        .gateway:hover .panel:not(:hover):not(:focus-visible) {
          flex: 0.72;
          opacity: 0.78;
        }
        .gateway:hover .panel:is(:hover, :focus-visible) {
          flex: 1.28;
          opacity: 1;
        }
      }

      .panel-divider {
        width: 1px;
        background: var(--sl-color-hairline);
        flex-shrink: 0;
        align-self: stretch;
      }

      .panel-badge {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0 0.75rem;
        background-color: var(--sl-color-bg-card);
        border: 1px solid var(--sl-color-hairline);
        border-radius: var(--sk-radius-sm, 0.25rem);
        font-family: var(--sl-font-mono, 'JetBrains Mono', monospace);
        font-size: 0.75rem;
        font-weight: 600;
        line-height: 1.125rem;
        text-transform: uppercase;
        color: var(--sl-color-text);
        text-align: center;
        white-space: nowrap;
        font-feature-settings: 'liga' 0, 'calt' 0;
        margin-bottom: 0;
      }

      .panel-label {
        font-size: clamp(1.625rem, 1.2rem + 1vw, 2.2rem);
        font-weight: 500;
        color: var(--sl-color-text-heading);
        line-height: 1.15;
        letter-spacing: -0.03em;
        max-width: 30rem;
        margin: 0;
      }

      /* SaaS headline: keep to two lines — wider measure + slightly smaller type than agents */
      .panel-saas .panel-label {
        max-width: 34rem;
        font-size: clamp(1.45rem, 1.05rem + 1vw, 2.05rem);
        text-wrap: pretty;
      }

      .panel-desc {
        font-size: 1rem;
        color: var(--sl-color-text-muted);
        line-height: 1.6;
        max-width: 24rem;
        margin: 0;
      }


      .panel-visual {
        width: min(100%, 36rem);
        margin: 0.1rem auto 0;
        border-radius: var(--sk-radius-xl, 0.75rem);
        border: none;
        background: transparent;
        box-shadow: none;
        overflow: hidden;
        text-align: left;
      }

      .panel-visual-header {
        padding: 0.8rem 0.95rem;
        border-bottom: 1px solid var(--sl-color-hairline);
        background: color-mix(in srgb, var(--sl-color-accent-low) 22%, white);
      }

      .panel-visual-title {
        display: block;
        font-size: 0.92rem;
        font-weight: 600;
        line-height: 1.3;
        color: var(--sl-color-text-heading);
      }

      .panel-visual-subtitle {
        display: block;
        margin-top: 0.2rem;
        font-size: 0.76rem;
        line-height: 1.45;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-body {
        display: grid;
        gap: 0.7rem;
        padding: 0;
      }

      .panel-visual-body img,
      .panel-visual-saas-body img {
        aspect-ratio: 920 / 440;
      }

      .panel-visual-node {
        border-radius: 0.9rem;
        border: 1px solid var(--sl-color-hairline);
        background: var(--sl-color-bg-card);
        padding: 0.8rem 0.9rem;
      }

      .panel-visual-label {
        display: block;
        margin-bottom: 0.25rem;
        font-size: 0.68rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-node strong {
        display: block;
        font-size: 0.92rem;
        line-height: 1.3;
        color: var(--sl-color-text-heading);
      }

      .panel-visual-node span {
        display: block;
        margin-top: 0.25rem;
        font-size: 0.78rem;
        line-height: 1.45;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-arrow {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
        font-weight: 700;
        line-height: 1;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-list {
        list-style: none;
        margin: 0;
        padding: 0;
        border-radius: 0.9rem;
        border: 1px solid var(--sl-color-hairline);
        background: var(--sl-color-bg-card);
        overflow: hidden;
      }

      .panel-visual-list li {
        padding: 0.7rem 0.9rem;
        border-top: 1px solid var(--sl-color-hairline);
      }

      .panel-visual-list li:first-child {
        border-top: 0;
      }

      .panel-visual-list strong {
        display: block;
        font-size: 0.82rem;
        line-height: 1.3;
        color: var(--sl-color-text-heading);
      }

      .panel-visual-list span {
        display: block;
        margin-top: 0.2rem;
        font-size: 0.75rem;
        line-height: 1.45;
        color: var(--sl-color-text-muted);
      }

      .panel-visual-chip-row {
        display: flex;
        flex-wrap: wrap;
        gap: 0.4rem;
      }

      .panel-visual-chip {
        display: inline-flex;
        align-items: center;
        padding: 0.26rem 0.56rem;
        border-radius: 999px;
        border: 1px solid var(--sl-color-hairline);
        background: color-mix(in srgb, var(--sl-color-bg-card) 90%, white);
        font-size: 0.72rem;
        font-weight: 500;
        color: var(--sl-color-text-muted);
        white-space: nowrap;
      }

      .panel-visual-saas {
        width: min(100%, 31rem);
      }

      .panel-visual-saas .panel-visual-header {
        background: color-mix(in srgb, var(--sl-color-accent-low) 28%, white);
      }

      .saas-arch {
        display: block;
      }

      .panel-cta {
        font-size: 0.95rem;
        font-weight: 600;
        margin-top: 0.25rem;
        color: var(--sl-color-accent);
      }

      @media (max-width: 1023px) and (min-width: 768px) {
        .panel {
          padding: 3rem 2rem;
        }
        .panel-label {
          max-width: 18rem;
        }
        /* Wider than agents column so the SaaS headline does not wrap to three lines */
        .panel-saas .panel-label {
          max-width: min(26rem, 100%);
        }
        .panel-desc {
          font-size: 0.95rem;
          max-width: 18rem;
        }
        .panel-visual {
          width: min(100%, 22rem);
        }
        .panel-visual-saas {
          width: min(100%, 25rem);
        }
      }

      @media (max-width: 767px) {
        .gateway {
          flex-direction: column;
          min-height: auto;
        }
        .panel-divider {
          width: 100%;
          height: 1px;
          align-self: auto;
        }
        .panel {
          flex: 1;
          padding: 2rem 1.5rem;
        }
        .panel-label {
          max-width: 100%;
        }
        .panel-saas .panel-label {
          font-size: clamp(1.28rem, 4.5vw + 0.65rem, 1.55rem);
        }
        .panel-desc {
          max-width: 100%;
        }
        .panel-visual {
          width: 100%;
        }
      }

      /* Desktop: hide illustrations so panels + CLI strip fill the viewport */
      @media (min-width: 1024px) {
        .panel-visual {
          display: none;
        }
      }

      .gateway-cli {
        text-align: center;
        padding: 1.5rem 1.5rem 1.75rem;
        margin: 0;
        background: var(--sl-color-bg);
        border-top: 1px solid var(--sl-color-hairline);
        flex-shrink: 0;
      }

      @keyframes gateway-fade-up {
        from { opacity: 0; transform: translateY(6px); }
        to   { opacity: 1; transform: translateY(0); }
      }

      .gateway-cli-steps {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        align-items: stretch;
        justify-content: center;
        gap: 0.75rem;
        flex-wrap: wrap;
      }

      .gateway-cli-step {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
        flex: 1 1 12rem;
        min-width: 11rem;
        max-width: 18rem;
        text-align: center;
        background: var(--sl-color-bg-card);
        border: 1px solid var(--sl-color-hairline);
        border-radius: var(--sk-radius-xl, 0.75rem);
        padding: 1rem 1.1rem;
        overflow: hidden;
        box-shadow: var(--sk-shadow-menu, 0 1px 2px rgba(10, 10, 10, 0.04));
      }

      @media (prefers-reduced-motion: no-preference) {
        .gateway-cli-step {
          opacity: 0;
          animation: gateway-fade-up 280ms ease-out forwards;
        }
        .gateway-cli-step:nth-child(1) { animation-delay: 0ms; }
        .gateway-cli-step:nth-child(2) { animation-delay: 70ms; }
        .gateway-cli-step:nth-child(3) { animation-delay: 140ms; }
      }

      .gateway-cli-step-num {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 1.5rem;
        height: 1.5rem;
        border-radius: var(--sk-radius-full, 50%);
        border: 1px solid var(--sl-color-accent);
        background: var(--sl-color-bg-card);
        font-size: 0.75rem;
        font-weight: 600;
        color: var(--sl-color-accent);
        flex-shrink: 0;
      }

      .gateway-cli-step-title {
        font-size: 0.9rem;
        font-weight: 500;
        color: var(--sl-color-text-heading);
        margin: 0;
      }

      .gateway-cli-step .expressive-code {
        width: 100%;
        max-width: 100%;
      }

      .gateway-cli-step .expressive-code pre {
        overflow-x: auto;
      }

      .gateway-cli-agent-icons {
        list-style: none;
        margin: 0.25rem 0 0;
        padding: 0;
        display: flex;
        gap: 0.5rem;
        justify-content: center;
      }

      .gateway-cli-agent-icons > li {
        margin: 0;
        padding: 0;
        list-style: none;
      }

      .gateway-cli-agent-icon {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 2rem;
        height: 2rem;
        border-radius: var(--sk-radius-md, 0.375rem);
        border: 1px solid var(--sl-color-hairline);
        background: var(--sl-color-bg);
        color: var(--sl-color-text-muted);
        font-size: 1rem;
      }

      .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border: 0;
      }

      .gateway-cli-step-prompt {
        font-size: 0.82rem;
        font-style: normal;
        color: var(--sl-color-text-muted);
        line-height: 1.45;
        max-width: 14rem;
        margin: 0;
        quotes: '"' '"';
      }

      .gateway-cli-step-prompt::before {
        content: open-quote;
      }

      .gateway-cli-step-prompt::after {
        content: close-quote;
      }

      /* Desktop: flow arrows between steps without polluting the ordered list */
      .gateway-cli-step:not(:last-child) {
        position: relative;
      }

      @media (min-width: 48rem) {
        .gateway-cli-steps {
          flex-wrap: nowrap;
          gap: 1.5rem;
        }
        .gateway-cli-step {
          flex: 1 1 0;
          min-width: 0;
        }
        .gateway-cli-step:not(:last-child)::after {
          content: '→';
          position: absolute;
          right: -1.15rem;
          top: 50%;
          transform: translateY(-50%);
          font-size: 1.1rem;
          color: var(--sl-color-gray-3);
          opacity: 0.55;
          pointer-events: none;
        }
      }

      @media (max-width: 47.99rem) {
        .gateway-cli {
          padding: 1.25rem 1rem 1.5rem;
        }
        .gateway-cli-steps {
          flex-direction: column;
          align-items: stretch;
        }
        .gateway-cli-step {
          max-width: none;
          flex: 1 1 auto;
        }
      }


---

import { Code } from '@astrojs/starlight/components'
import IconClaude from '~icons/simple-icons/claude'
import IconCopilot from '~icons/simple-icons/githubcopilot'
import IconCursor from '~icons/simple-icons/cursor'
import IconCodex from '~icons/simple-icons/openai'
import agentkitHero from '../../assets/pages/hero/agentkit.svg'
import authForSaasHero from '../../assets/pages/hero/auth-for-saas.svg'



<div class="gateway-shell not-content">
  <h1 class="sr-only">Choose your Scalekit documentation path</h1>

  <div class="gateway">
    <a
      href="/agentkit/quickstart/"
      class="panel panel-agents"
    >
      <span class="panel-badge">For Agent Builders</span>
      <p class="panel-label">Connect my agents to any enterprise app</p>
      <p class="panel-desc">Delegated auth. Scoped permissions. Tool calls.</p>
      <span class="panel-cta">Connect my agents to apps →</span>
      <div class="panel-visual" aria-hidden="true">
        <div class="panel-visual-body">
          <img
            src={agentkitHero.src}
            alt="Agent authentication flow diagram"
            width={920}
            height={440}
            loading="lazy"
            decoding="async"
            style="width: 100%; height: auto;"
          />
        </div>
      </div>
    </a>

    <div class="panel-divider" role="separator"></div>

    <a
      href="/authenticate/fsa/quickstart/"
      class="panel panel-saas"
    >
      <span class="panel-badge">For SaaS Developers</span>
      <p class="panel-label">{`Add auth and user\u00a0management to my SaaS app`}</p>
      <p class="panel-desc">Sessions, SSO, RBAC, SCIM - all in one stack.</p>
      <span class="panel-cta">Add auth to my app →</span>
      <div class="panel-visual panel-visual-saas" aria-hidden="true">
        <div class="panel-visual-saas-body">
          <div class="saas-arch">
            <img
              src={authForSaasHero.src}
              alt="Authentication architecture overview"
              width={920}
              height={440}
              loading="lazy"
              decoding="async"
              style="width: 100%; height: auto;"
            />
          </div>
        </div>
      </div>
    </a>
  </div>

  <section class="gateway-cli not-content" aria-labelledby="gateway-cli-heading">
    <h2 class="gateway-section-label" id="gateway-cli-heading">Try it in 3 steps</h2>
    <ol class="gateway-cli-steps">
      <li class="gateway-cli-step">
        <span class="gateway-cli-step-num" aria-hidden="true">1</span>
        <p class="gateway-cli-step-title">Run the CLI</p>
        <Code
          code="npx @scalekit-inc/cli setup"
          lang="sh"
          frame="none"
          showLineNumbers={false}
          wrap
        />
      </li>
      <li class="gateway-cli-step">
        <span class="gateway-cli-step-num" aria-hidden="true">2</span>
        <p class="gateway-cli-step-title">Choose your coding agent</p>
        <ul class="gateway-cli-agent-icons" aria-label="Supported coding agents">
          <li>
            <span class="gateway-cli-agent-icon" title="Claude Code">
              <IconClaude aria-hidden="true" />
              <span class="sr-only">Claude Code</span>
            </span>
          </li>
          <li>
            <span class="gateway-cli-agent-icon" title="GitHub Copilot">
              <IconCopilot aria-hidden="true" />
              <span class="sr-only">GitHub Copilot</span>
            </span>
          </li>
          <li>
            <span class="gateway-cli-agent-icon" title="Cursor">
              <IconCursor aria-hidden="true" />
              <span class="sr-only">Cursor</span>
            </span>
          </li>
          <li>
            <span class="gateway-cli-agent-icon" title="Codex">
              <IconCodex aria-hidden="true" />
              <span class="sr-only">Codex</span>
            </span>
          </li>
        </ul>
      </li>
      <li class="gateway-cli-step">
        <span class="gateway-cli-step-num" aria-hidden="true">3</span>
        <p class="gateway-cli-step-title">Ask it</p>
        <p class="gateway-cli-step-prompt">Analyze my project and tell me what problems Scalekit can solve for me</p>
      </li>
    </ol>
  </section>
</div>
