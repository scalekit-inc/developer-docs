---
title: 'Coding agents: Add SSO to your app'
description: 'Let your coding agents guide you into adding Scalekit SSO to your existing application in minutes'
tags: [saaskit, sso, quickstart]
sidebar:
  label: 'Modular SSO'
tableOfContents: false
head:
  - tag: style
    content: |
      .sl-markdown-content h2 {
        font-size: var(--sl-text-xl);
      }
      .sl-markdown-content h3 {
        font-size: var(--sl-text-lg);
      }
next:
  label: 'Modular SSO implementation guide'
  link: '/authenticate/sso/add-modular-sso/'
seeAlso:
  label: 'See also'
  expanded: true
  items:
    - title: 'Browse SSO code samples'
      icon: 'github'
      url: 'https://github.com/scalekit-inc/scalekit-express-example'
---

import { Steps, Aside } from '@astrojs/starlight/components'
import SupportedAgents from '@components/ui/SupportedAgents.astro'

Use AI coding agents to add Scalekit's Modular SSO to your existing applications. The agent analyzes your codebase, applies SSO patterns, and generates production-ready code that integrates enterprise identity providers.

```bash showLineNumbers=false frame="none"
npx @scalekit-inc/cli setup
```

<SupportedAgents hideCommand />

<Steps>
1. ## Generate SSO implementation

   Copy this prompt into your coding agent:

   ```md wrap showLineNumbers=false title="SSO implementation prompt"
   Add Scalekit SSO to my app — initialize ScalekitClient, generate an SSO authorization URL for a given organization, handle the SSO callback to validate and exchange the code for user identity, and integrate the SSO user into my existing session system. Code only.
   ```

   <Aside type="caution" title="Review generated code">
   Always review AI-generated authentication code before deployment. Verify that environment variables, token validation logic, and error handling match your security requirements.
   </Aside>

2. ## Verify the implementation

   Check that generated files include:
   - Scalekit client initialization with environment credentials
   - SSO authorization URL generation for organization-based routing
   - SSO callback handler that validates the authorization code and retrieves user identity
   - Integration logic that maps SSO user identity into your existing session system

</Steps>
