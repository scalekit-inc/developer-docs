---
title: Attio
description: Connect to Attio CRM to manage contacts, companies, deals, notes, tasks, and lists with a modern relationship management platform.
tableOfContents: true
head:
  - tag: style
    content: |
      .sl-markdown-content h2 {
        font-size: var(--sl-text-xl);
      }
      table td:first-child, table th:first-child {
        white-space: nowrap;
      }
      /* Heading copy functionality */
      .sl-markdown-content h2,
      .sl-markdown-content h3,
      .sl-markdown-content h4 {
        position: relative;
        cursor: pointer;
      }
      .sl-markdown-content h2:hover::after,
      .sl-markdown-content h3:hover::after,
      .sl-markdown-content h4:hover::after {
        content: " 📋 ";
        font-size: 0.8em;
        opacity: 0.7;
      }
  - tag: script
    content: |
      (function() {
        function initHeadingCopy() {
          const headings = document.querySelectorAll('.sl-markdown-content h2, .sl-markdown-content h3, .sl-markdown-content h4');
          headings.forEach(heading => {
            if (heading.hasAttribute('data-copy-initialized')) return;
            heading.setAttribute('data-copy-initialized', 'true');
            heading.addEventListener('click', async () => {
              const headingText = heading.textContent.replace(/\s📋$/, '').trim();
              try {
                await navigator.clipboard.writeText(headingText);
                const originalText = heading.textContent;
                heading.textContent = heading.textContent.replace(/\s📋$/, '') + ' ✓';
                setTimeout(() => { heading.textContent = originalText; }, 2000);
              } catch (err) {
                console.error('Failed to copy heading:', err);
              }
            });
          });
        }
        document.addEventListener('DOMContentLoaded', initHeadingCopy);
        if (document.readyState === 'complete' || document.readyState === 'interactive') {
          setTimeout(initHeadingCopy, 1);
        }
        document.addEventListener('astro:page-load', initHeadingCopy);
      })();
---

import { Card, CardGrid, Tabs, TabItem, Badge, Steps, Aside, Code } from '@astrojs/starlight/components'
import { Accordion, AccordionItem } from 'accessible-astro-components'
import { SetupAttioSection } from '@components/templates'

<div class="grid grid-cols-5 gap-4 items-center">
 <div class="col-span-4">
  Connect to Attio to manage CRM records, people, companies, deals, tasks, notes, and workspace data
 </div>
 <div class="flex justify-center">
  <img src="https://cdn.scalekit.com/sk-connect/assets/provider-icons/attio.svg" width="64" height="64" alt="Attio logo" />
 </div>
</div>

Supports authentication: <Badge text="OAuth 2.0" />

## Set up the agent connector

<SetupAttioSection />

## Usage

Connect a user's Attio workspace and make API calls on their behalf — Scalekit handles OAuth and token management automatically.

<Tabs syncKey="tech-stack">
  <TabItem label="Python">
    ```python
    import scalekit.client, os
    from dotenv import load_dotenv
    load_dotenv()

    connection_name = "attio"  # connection name from Scalekit dashboard
    identifier = "user_123"    # your unique user identifier

    # Get credentials from app.scalekit.com → Developers → API Credentials
    scalekit_client = scalekit.client.ScalekitClient(
        client_id=os.getenv("SCALEKIT_CLIENT_ID"),
        client_secret=os.getenv("SCALEKIT_CLIENT_SECRET"),
        env_url=os.getenv("SCALEKIT_ENV_URL"),
    )

    # Get authorization link and send it to your user
    link_response = scalekit_client.actions.get_authorization_link(
        connection_name=connection_name,
        identifier=identifier
    )
    print("Authorize Attio:", link_response.link)

    # After the user authorizes, make API calls via Scalekit proxy
    result = scalekit_client.actions.request(
        connection_name=connection_name,
        identifier=identifier,
        path="/v2/objects/people/records/query",
        method="POST"
    )
    print(result)
    ```
  </TabItem>
</Tabs>

## Tool list

## `attio_create_person`

Create a new person record in Attio. Throws an error on conflicts of unique attributes like `email_addresses`. Use `attio_assert_person` if you prefer to update on conflicts.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `values` | object | Yes | Attribute values as a JSON object mapping attribute slugs to their values |

## `attio_list_people`

List person records in Attio with optional filtering and sorting. Returns paginated results.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `filter` | object | No | Filter conditions to narrow results by attribute values |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of records to return per page |
| `offset` | integer | No | Number of records to skip for pagination |

## `attio_get_person`

Retrieve a single person record by its `record_id`. Returns all attribute values with temporal and audit metadata.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the person record to retrieve |

## `attio_delete_person`

Permanently delete a person record from Attio by its `record_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the person record to delete |

## `attio_create_company`

Create a new company record in Attio. Throws an error on conflicts of unique attributes like `domains`. Use `attio_assert_company` if you prefer to update on conflicts.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `values` | object | Yes | Attribute values as a JSON object mapping attribute slugs to their values |

## `attio_list_companies`

List company records in Attio with optional filtering and sorting. Returns paginated results.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `filter` | object | No | Filter conditions to narrow results by attribute values |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of records to return per page |
| `offset` | integer | No | Number of records to skip for pagination |

## `attio_get_company`

Retrieve a single company record by its `record_id`. Returns all attribute values with temporal and audit metadata.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the company record to retrieve |

## `attio_delete_company`

Permanently delete a company record from Attio by its `record_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the company record to delete |

## `attio_create_deal`

Create a new deal record in Attio. Throws an error on conflicts of unique attributes. Provide at least one attribute value.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `values` | object | Yes | Attribute values as a JSON object mapping attribute slugs to their values |

## `attio_list_deals`

List deal records in Attio with optional filtering and sorting. Returns paginated results.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `filter` | object | No | Filter conditions to narrow results by attribute values |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of records to return per page |
| `offset` | integer | No | Number of records to skip for pagination |

## `attio_get_deal`

Retrieve a single deal record by its `record_id`. Returns all attribute values with temporal and audit metadata.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the deal record to retrieve |

## `attio_delete_deal`

Permanently delete a deal record from Attio by its `record_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the deal record to delete |

## `attio_create_record`

Create a new record in Attio for a given object type (e.g. people, companies, deals). Throws an error if a unique attribute conflict is detected.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug or UUID (e.g., `people`, `companies`, `deals`) |
| `values` | object | Yes | Attribute values as a JSON object mapping attribute slugs to their values |

## `attio_list_records`

List and query records for a specific Attio object type. Supports filtering by attribute values, sorting, and pagination. Returns guaranteed up-to-date data.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug or UUID (e.g., `people`, `companies`, `deals`) |
| `filter` | object | No | Filter conditions to narrow results by attribute values |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of records to return per page |
| `offset` | integer | No | Number of records to skip for pagination |

## `attio_search_records`

Search for records in Attio for a given object type using a fuzzy text query. Returns matching records with their IDs, labels, and key attributes.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug (e.g., `people`, `companies`, `deals`) |
| `query` | string | Yes | Fuzzy text query to match against records |
| `limit` | integer | No | Number of results to return |
| `offset` | integer | No | Number of results to skip for pagination |

## `attio_get_record`

Retrieve a specific record from Attio by its object type and record ID. Returns the full record including all attribute values with their complete audit trail.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug or UUID |
| `record_id` | string | Yes | ID of the record to retrieve |

## `attio_delete_record`

Permanently delete a record from Attio by its object type and record ID. This action is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug or UUID |
| `record_id` | string | Yes | ID of the record to delete |

## `attio_get_record_attribute_values`

Retrieve all values for a given attribute on a record in Attio. Can include historic values using the `show_historic` parameter. Not available for COMINT or enriched attributes.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug or UUID |
| `record_id` | string | Yes | ID of the record |
| `attribute` | string | Yes | Attribute slug or UUID to retrieve values for |
| `show_historic` | boolean | No | Set to `true` to include historic attribute values |

## `attio_create_task`

Create a new task in Attio. Tasks can be linked to one or more records and assigned to workspace members. Only plaintext format is supported for task content.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `content` | string | Yes | Task content in plaintext |
| `deadline_at` | string | No | ISO 8601 deadline timestamp |
| `is_completed` | boolean | No | Initial completion status (default: `false`) |
| `linked_records` | array | No | Records to link to this task (object type and record ID pairs) |
| `assignees` | array | No | Workspace member UUIDs to assign the task to |

## `attio_list_tasks`

List tasks in Attio, optionally filtered by linked record. Returns tasks with their content, deadline, completion status, assignees, and linked records.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linked_object` | string | No | Object type slug to filter tasks by linked record type |
| `linked_record_id` | string | No | Record ID to filter tasks linked to a specific record |
| `limit` | integer | No | Number of tasks to return |
| `offset` | integer | No | Number of tasks to skip for pagination |

## `attio_get_task`

Retrieve a single task by its `task_id`. Returns the task's content, deadline, assignees, and linked records.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `task_id` | string | Yes | ID of the task to retrieve |

## `attio_delete_task`

Permanently delete a task from Attio by its `task_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `task_id` | string | Yes | ID of the task to delete |

## `attio_create_note`

Create a note on an Attio record (person, company, deal, or custom object). Notes support plaintext or Markdown formatting. Optionally backdate the note or associate it with a meeting.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `parent_object` | string | Yes | Object type slug of the record to attach the note to |
| `parent_record_id` | string | Yes | ID of the record to attach the note to |
| `title` | string | No | Note title |
| `content` | string | No | Note content in plaintext or Markdown |
| `format` | string | No | Content format: `plaintext` or `markdown` |
| `created_at` | string | No | ISO 8601 timestamp to backdate the note |
| `meeting_id` | string | No | ID of an existing meeting to associate with the note |

## `attio_list_notes`

List notes in Attio. Optionally filter by a parent object and record. Supports pagination with a maximum of 50 results per page.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `parent_object` | string | No | Object type slug to filter notes by record type |
| `parent_record_id` | string | No | Record ID to filter notes attached to a specific record |
| `limit` | integer | No | Number of notes to return (max 50) |
| `offset` | integer | No | Number of notes to skip for pagination |

## `attio_get_note`

Retrieve a single note by its `note_id`. Returns the note's title, content (plaintext and Markdown), tags, and creator information.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `note_id` | string | Yes | ID of the note to retrieve |

## `attio_delete_note`

Permanently delete a note from Attio by its `note_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `note_id` | string | Yes | ID of the note to delete |

## `attio_create_comment`

Create a new comment on a record or list entry. To comment on a record, provide `record_object` and `record_id`. To comment on a list entry, provide `list_id` and `entry_id`. To reply to an existing thread, provide `thread_id`. Content is always plaintext.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `author_actor_id` | string | Yes | Workspace member UUID of the comment author |
| `content` | string | Yes | Comment content in plaintext |
| `record_object` | string | No | Object type slug when commenting on a record |
| `record_id` | string | No | Record ID when commenting on a record |
| `list_id` | string | No | List ID when commenting on a list entry |
| `entry_id` | string | No | Entry ID when commenting on a list entry |
| `thread_id` | string | No | Thread ID to reply to an existing comment thread |

## `attio_get_comment`

Retrieve a single comment by its `comment_id`. Returns the comment's content, author, thread, and resolution status.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `comment_id` | string | Yes | ID of the comment to retrieve |

## `attio_delete_comment`

Permanently delete a comment by its `comment_id`. If the comment is at the head of a thread, all messages in the thread are also deleted.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `comment_id` | string | Yes | ID of the comment to delete |

## `attio_list_threads`

List threads of comments on a record or list entry. Returns all comment threads associated with a specific record or list entry.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_object` | string | No | Object type slug when listing threads for a record |
| `record_id` | string | No | Record ID when listing threads for a record |
| `list_id` | string | No | List ID when listing threads for a list entry |
| `entry_id` | string | No | Entry ID when listing threads for a list entry |

## `attio_list_lists`

Retrieve all CRM lists available in the Attio workspace, along with entries for a specific record. Lists are used to track pipeline stages, outreach targets, or custom groupings of records.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `parent_record_id` | string | No | Filter entries by this parent record ID |
| `parent_object` | string | No | Object type slug to filter entries by record type |

## `attio_create_list`

Create a new list in Attio. After creation, add attributes using `attio_create_attribute` and records using `attio_add_to_list`.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | string | Yes | Name of the list |
| `api_slug` | string | Yes | URL-safe identifier for the list |
| `workspace_access` | string | Yes | Access level: `full-access`, `read-and-write`, or `read-only` |
| `workspace_member_access` | array | Yes | Array of workspace member access configurations |

## `attio_get_list`

Retrieve details of a single list in the Attio workspace by its UUID or slug.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `list` | string | Yes | List UUID or slug |

## `attio_list_entries`

List entries in a given Attio list with optional filtering and sorting. Returns records that belong to the specified list.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `list` | string | Yes | List UUID or slug |
| `filter` | object | No | Filter conditions to narrow results |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of entries to return |
| `offset` | integer | No | Number of entries to skip for pagination |

## `attio_get_list_entry`

Retrieve a single list entry by its `entry_id`. Returns detailed information about a specific entry in an Attio list.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `list` | string | Yes | List UUID or slug |
| `entry_id` | string | Yes | ID of the list entry to retrieve |

## `attio_add_to_list`

Add a record (contact, company, deal, or custom object) to a specific Attio list. Returns the newly created list entry with its entry ID. If the record is already in the list, a new entry is created.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `list` | string | Yes | List UUID or slug |
| `record_id` | string | Yes | ID of the record to add to the list |

## `attio_remove_from_list`

Remove a specific entry from an Attio list by its entry ID. This deletes the list entry but does not delete the underlying record. Returns 404 if the entry does not exist.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `list` | string | Yes | List UUID or slug |
| `entry_id` | string | Yes | ID of the list entry to remove |

## `attio_list_record_entries`

List all entries across all lists for which a specific record is the parent. Returns list IDs, slugs, entry IDs, and creation timestamps.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object type slug or UUID |
| `record_id` | string | Yes | ID of the record |

## `attio_list_objects`

Retrieve all available objects (both system-defined and user-defined) in the Attio workspace. Useful for understanding the workspace structure before filtering or writing.

| Name | Type | Required | Description |
| --- | --- | --- | --- |

## `attio_get_object`

Retrieve details of a single object by its slug or UUID in Attio.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | Yes | Object slug or UUID |

## `attio_create_object`

Create a new custom object in the Attio workspace. Use when you need an object type beyond the standard types (people, companies, deals, users, workspaces).

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `singular_noun` | string | Yes | Singular display name for the object (e.g., `Project`) |
| `plural_noun` | string | Yes | Plural display name for the object (e.g., `Projects`) |
| `api_slug` | string | Yes | URL-safe identifier for the object |

## `attio_list_attributes`

List the attribute schema for an Attio object or list, including slugs, types, and select/status configuration. Use to discover what attributes exist and their types before filtering or writing.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | No | Object slug or UUID to list attributes for |
| `list` | string | No | List slug or UUID to list attributes for |

## `attio_get_attribute`

Retrieve details of a single attribute on an Attio object or list, including its type, slug, configuration, and metadata.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | No | Object slug or UUID containing the attribute |
| `list` | string | No | List slug or UUID containing the attribute |
| `attribute` | string | Yes | Attribute slug or UUID |

## `attio_create_attribute`

Create a new attribute on an Attio object or list. The `config` object varies by type — pass `{}` for most types; for `select`/`multiselect`, config can include options; for `record-reference`, config includes the target object.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | No | Object slug or UUID to add the attribute to |
| `list` | string | No | List slug or UUID to add the attribute to |
| `api_slug` | string | Yes | URL-safe identifier for the attribute |
| `title` | string | Yes | Display name for the attribute |
| `type` | string | Yes | Attribute type (e.g., `text`, `number`, `select`, `record-reference`) |
| `description` | string | Yes | Description of the attribute |
| `is_required` | boolean | Yes | Whether the attribute is required |
| `is_unique` | boolean | Yes | Whether the attribute value must be unique |
| `is_multiselect` | boolean | Yes | Whether multiple values are allowed |
| `config` | object | Yes | Type-specific configuration object |

## `attio_list_attribute_options`

List all select options for a `select` or `multiselect` attribute on an Attio object or list.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | No | Object slug or UUID containing the attribute |
| `list` | string | No | List slug or UUID containing the attribute |
| `attribute` | string | Yes | Attribute slug or UUID |

## `attio_list_attribute_statuses`

List all statuses for a `status` attribute on an Attio object or list. Returns status IDs, titles, and configuration.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `object` | string | No | Object slug or UUID containing the attribute |
| `list` | string | No | List slug or UUID containing the attribute |
| `attribute` | string | Yes | Attribute slug or UUID |

## `attio_list_workspace_members`

List all workspace members in the Attio workspace. Use to retrieve workspace member IDs needed for assigning owners or actor-reference attributes.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `limit` | integer | No | Number of members to return |
| `offset` | integer | No | Number of members to skip for pagination |

## `attio_get_workspace_member`

Retrieve a single workspace member by their `workspace_member_id`. Returns name, email, access level, and avatar information.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `workspace_member_id` | string | Yes | ID of the workspace member to retrieve |

## `attio_list_user_records`

List user records in Attio with optional filtering and sorting. Returns paginated results.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `filter` | object | No | Filter conditions to narrow results by attribute values |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of records to return |
| `offset` | integer | No | Number of records to skip for pagination |

## `attio_delete_user_record`

Permanently delete a user record from Attio by its `record_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the user record to delete |

## `attio_list_workspace_records`

List workspace records in Attio with optional filtering and sorting. Returns paginated results.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `filter` | object | No | Filter conditions to narrow results by attribute values |
| `sorts` | array | No | Sort order for results |
| `limit` | integer | No | Number of records to return |
| `offset` | integer | No | Number of records to skip for pagination |

## `attio_get_workspace_record`

Retrieve a single workspace record by its `record_id`. Returns all attribute values with temporal and audit metadata.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the workspace record to retrieve |

## `attio_delete_workspace_record`

Permanently delete a workspace record from Attio by its `record_id`. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `record_id` | string | Yes | ID of the workspace record to delete |

## `attio_list_webhooks`

Retrieve all webhooks in the Attio workspace. Returns webhook configurations, subscriptions, and statuses. Supports optional limit and offset pagination.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `limit` | integer | No | Number of webhooks to return |
| `offset` | integer | No | Number of webhooks to skip for pagination |

## `attio_get_webhook`

Retrieve a single webhook by its `webhook_id`. Returns the webhook's target URL, event subscriptions, status, and metadata.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `webhook_id` | string | Yes | ID of the webhook to retrieve |

## `attio_delete_webhook`

Permanently delete a webhook by its `webhook_id` from Attio. This operation is irreversible.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `webhook_id` | string | Yes | ID of the webhook to delete |

## `attio_list_meetings`

List all meetings in the Attio workspace. Optionally filter by participants or linked records.

<Aside type="caution">
This tool is in beta and may change without notice.
</Aside>

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `filter` | object | No | Filter conditions (e.g., by participants or linked record IDs) |
| `limit` | integer | No | Number of meetings to return |
| `offset` | integer | No | Number of meetings to skip for pagination |

## `attio_get_current_token_info`

Identify the current access token, the workspace it is linked to, and its permissions. Use to verify token validity or retrieve workspace information.

| Name | Type | Required | Description |
| --- | --- | --- | --- |
