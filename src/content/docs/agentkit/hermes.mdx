---
title: 'Connect Hermes to AgentKit'
description: 'Wire Hermes Agent to Gmail, Slack, and other apps through Scalekit so the agent can act as the user.'
tags: [agentkit, hermes, nous, ai-agents, tools, integrations]
sidebar:
  label: 'Hermes'
  prev:
    label: 'OpenClaw'
    link: '/agentkit/openclaw'
  next:
    label: 'AgentKit examples'
    link: '/agentkit/examples'
seeAlso:
  expanded: true
  items:
    - title: 'Hermes Agent docs'
      icon: 'book'
      url: 'https://hermes-agent.nousresearch.com/docs/'
    - title: 'OpenClaw how-to'
      icon: 'book'
      url: '/agentkit/openclaw'
    - title: 'Host skill on GitHub'
      icon: 'github'
      url: 'https://github.com/scalekit-inc/authstack/tree/main/kits/agentkit/skills/integrate-agentkit-host'
    - title: 'Hermes Slack gateway'
      icon: 'book'
      url: 'https://hermes-agent.nousresearch.com/docs/user-guide/messaging/slack'
    - title: 'Hermes cron'
      icon: 'book'
      url: 'https://hermes-agent.nousresearch.com/docs/user-guide/features/cron'
head:
  - tag: style
    content: |
      .sl-markdown-content h2 { font-size: var(--sl-text-xl); }
      .sl-markdown-content h3 { font-size: var(--sl-text-lg); }
tableOfContents: true
---

import { Steps, Tabs, TabItem, CardGrid, LinkCard, Badge } from '@astrojs/starlight/components';

Scalekit lets a Hermes agent act as your user in Gmail, Slack, Linear, Salesforce, and other apps. Scalekit holds the connected account for each app the user authorized, then the agent calls tools with that account — it can do anything the user can do there.

Hermes does not ship this wiring — add it as a skill. Scalekit publishes the skill as `integrate-agentkit-host`; copy its runtime files into Hermes under the name `scalekit-agent-auth`. Without `scalekit-agent-auth`, Hermes cannot look up Scalekit connections, send a magic link, or run tools as the user.

```d2
direction: right

Hermes: "Hermes Agent" {
  style.font-size: 18
}

Scalekit: {
  label: "Scalekit AgentKit"
  Auth: "OAuth handler"
  Vault: "Token vault"
}

Providers: {
  label: "Third-party services"
  Gmail: "Gmail"
  Slack: "Slack"
  Salesforce: "Salesforce"
  More: "200+ more"
}

Hermes -> Scalekit.Auth: "Execute tools"
Scalekit.Vault -> Providers.Gmail
Scalekit.Vault -> Providers.Slack
Scalekit.Vault -> Providers.Salesforce
Scalekit.Vault -> Providers.More
```

When you ask Hermes to use a third-party app, `scalekit-agent-auth`:

- Finds the Scalekit connection for that action (for example [Gmail](/agentkit/connectors/gmail/))
- Checks that the user's connected account is `ACTIVE`, which means Scalekit holds a working token for it
- Returns a magic link when the account is not `ACTIVE`. A magic link is a one-time URL the user opens to authorize the app. An API key connection points you to the Dashboard instead
- Fetches the tool schema, calls the tool, and returns the result
- Falls back to the Scalekit HTTP proxy when the connection exposes no named tool for the action

## Prerequisites

- [Hermes Agent](https://hermes-agent.nousresearch.com/docs/getting-started/quickstart) installed
- A Scalekit account with AgentKit enabled: [sign up at app.scalekit.com](https://app.scalekit.com)
- At least one connection in **Dashboard > AgentKit > Connections** (for example, Gmail)
- `python3` and [`uv`](https://docs.astral.sh/uv/), the Python package manager, on your `PATH`

## Install the skill on Hermes

<Steps>
1. ### Copy the runtime skill onto the Hermes profile

   Clone [authstack](https://github.com/scalekit-inc/authstack/tree/main/kits/agentkit/skills/integrate-agentkit-host), then copy the runtime file and its scripts into a Hermes skill folder:

   ```bash showLineNumbers=false
   git clone --depth 1 https://github.com/scalekit-inc/authstack.git
   cd authstack/kits/agentkit/skills/integrate-agentkit-host

   DEST="${HERMES_HOME:-$HOME/.hermes}/skills/scalekit-agent-auth"
   mkdir -p "$DEST/scripts"
   cp references/runtime-skill.md "$DEST/SKILL.md"
   cp scripts/tool_exec.py scripts/pyproject.toml "$DEST/scripts/"
   cd "$DEST/scripts" && uv sync
   ```

   The [skills pack](https://github.com/scalekit-inc/skills/tree/main/skills/integrate-agentkit-host) carries the same skill if you prefer that repo.

   Run `hermes skills list`. The table lists `scalekit-agent-auth` with status `enabled`:

   ```txt showLineNumbers=false
   │ scalekit-agent-auth │          │ local  │ local │ enabled │
   ```

   If the row is missing, or the script fails to import, run `uv sync` again from `$DEST/scripts`.

   Copy the files — do not run `hermes skills install` on `integrate-agentkit-host`, and do not install from a raw `SKILL.md` URL. Hermes needs `runtime-skill.md` as the skill file, plus `scripts/`.

   Hermes now answers to `/scalekit-agent-auth`. New chats pick the skill up on their own. Run `/reset` to load it in the chat you already have open.

2. ### Configure credentials

   Put only Scalekit client credentials in `~/.hermes/.env`. Gmail and Slack tokens never go in that file. Scalekit stores the provider tokens and refreshes them.

   ```bash title="~/.hermes/.env"
   SCALEKIT_CLIENT_ID=skc_your_client_id
   SCALEKIT_CLIENT_SECRET=your_client_secret  # Never hardcode secrets; use environment variables.
   SCALEKIT_ENVIRONMENT_URL=https://your-env.scalekit.cloud
   SCALEKIT_IDENTIFIER=your_default_user_identifier
   ```

   | Parameter | Description |
   |-----------|-------------|
   | `SCALEKIT_CLIENT_ID` | Your Scalekit client ID <Badge text="Required" /> |
   | `SCALEKIT_CLIENT_SECRET` | Your Scalekit client secret <Badge text="Required" /> |
   | `SCALEKIT_ENVIRONMENT_URL` | Your Scalekit environment URL <Badge text="Required" /> |
   | `SCALEKIT_IDENTIFIER` | Default user for tool calls <Badge text="Recommended" /> |

   Copy the client ID, client secret, and environment URL from **Dashboard > Developers > Settings > API Credentials**. The identifier is not a dashboard credential. Set it to the user you want Hermes to act as, for example `alice`.

3. ### Ask Hermes to act

   Start a chat and ask for a real action:

   <Tabs>
   <TabItem label="Gmail">
   ```txt showLineNumbers=false
   You: Show me my latest unread emails
   ```

   Hermes loads the skill and:

   1. Looks up the Gmail connection
   2. Checks that the connected account is `ACTIVE` (or gives you a magic link)
   3. Fetches the tool schema
   4. Calls the tool and returns the mail
   </TabItem>
   <TabItem label="Notion">
   ```txt showLineNumbers=false
   You: Read my Notion page https://notion.so/My-Page-abc123
   ```

   Hermes:

   1. Looks up the Notion connection
   2. Gives you a magic link if you have not authorized yet
   3. Fetches the page tool schema
   4. Returns the page content
   </TabItem>
   <TabItem label="Slack (as a user)">
   ```txt showLineNumbers=false
   You: As identifier alice, list my unread Slack DMs
   ```

   Hermes:

   1. Looks up the Slack **user** connection
   2. Gives you a magic link if the Slack user account is not `ACTIVE`
   3. Fetches the Slack tool schema
   4. Returns the direct messages (DMs) as `alice`, not as the Hermes bot
   </TabItem>
   <TabItem label="Google Calendar">
   ```txt showLineNumbers=false
   You: Create an out-of-office event tomorrow on alice's calendar
   ```

   Hermes:

   1. Looks up the Google Calendar connection for `alice`
   2. Gives you a magic link if needed
   3. Calls the create-event tool
   4. Scalekit writes the event with `alice`'s token
   </TabItem>
   </Tabs>

   You can also force the skill:

   ```txt showLineNumbers=false
   /scalekit-agent-auth show my unread emails
   ```

</Steps>

## Verify it works

Confirm all of the following:

- The skill is listed (`hermes skills list` or `/skills`)
- The connected account is `ACTIVE` after you finish the magic link
- Hermes returns data from the provider (unread mail, or the Notion page)

Open the magic link again and retry the same prompt if the account stays inactive.

## Use Slack as a bot or as a user

The Slack gateway and the Slack user connection are two identities. Do not mix them.

| Job | Use |
|-----|-----|
| Chat with the agent in a channel or DM | Hermes Slack **gateway**. Bot tokens (`xoxb-` + `xapp-`) in `~/.hermes/.env`. See the [Hermes Slack setup](https://hermes-agent.nousresearch.com/docs/user-guide/messaging/slack). |
| Read private history, post, or act as a named person | Scalekit Slack **user** connection on that identifier |

The gateway bot is a channel. It is not `alice`.

A typical always-on setup uses both: the gateway hears the channel, and Scalekit acts as the user.

## Run jobs on a schedule

Attach the skill to a Hermes cron job. Each fire is a fresh session. The skill uses Scalekit client credentials. You do not mint a session token.

```bash showLineNumbers=false
hermes cron create "0 9 * * *" "List alice's unread emails and post a 5-line summary" --skill scalekit-agent-auth
```

Or in chat:

```txt showLineNumbers=false
/cron add "every weekday at 9am" "List alice's unread emails and post a 5-line summary" --skill scalekit-agent-auth
```

The connected account must already be `ACTIVE`. Cron cannot click a magic link.

Keep the gateway running on a headless host so a new magic link can land in Slack or Telegram. Do not put Gmail or Calendar refresh tokens in `~/.hermes/.env`.

## Avoid these mistakes

- Do not put provider tokens in `~/.hermes/.env`. Put only Scalekit client credentials there.
- Do not use the bundled Google Workspace skill (`~/.hermes/google_token.json`) if AgentKit owns the Google user. `~/.hermes/google_token.json` is one laptop login, not a named identifier.
- Do not run `hermes mcp login` against Scalekit Virtual MCP (a Scalekit MCP URL plus a session token). Virtual MCP uses a static bearer session token. It is not MCP OAuth.
- Do not treat the Slack bot token as “send as alice”.
- Do not use a Hermes host as a multi-tenant identity provider. One gateway is one operator. Switch identifiers on the same host. Do not mint per-customer session tokens inside Hermes.

## Next steps

<CardGrid>
  <LinkCard
    title="Browse connections"
    description="See supported connectors in the Scalekit dashboard"
    href="/agentkit/connectors/"
  />
  <LinkCard
    title="Authorize a user"
    description="How magic links and connected accounts work"
    href="/agentkit/tools/authorize/"
  />
  <LinkCard
    title="OpenClaw how-to"
    description="The same AgentKit skill on OpenClaw"
    href="/agentkit/openclaw"
  />
</CardGrid>

## Fix common errors

<details>
<summary>How do I authorize a new connection?</summary>

The skill returns a magic link if the account is not `ACTIVE`. Open the link. Finish OAuth. Return to Hermes and retry.

Set the key in **Dashboard > Connections** for API key connections (for example Snowflake). The skill cannot complete the API key flow in chat.

</details>

<details>
<summary>How do I switch users?</summary>

Set `SCALEKIT_IDENTIFIER` in `~/.hermes/.env` as the default.

Name the identifier in the prompt to act as someone else for one turn, for example `as identifier alice`. Scalekit scopes tools to the named user's connected account.

</details>

<details>
<summary>Why do I see "connection not found"?</summary>

1. Confirm the connection exists in **Dashboard > AgentKit > Connections**
2. Confirm it is complete, not a draft
3. Confirm `SCALEKIT_ENVIRONMENT_URL` matches the environment that holds the connection

</details>

<details>
<summary>The connected account is not ACTIVE</summary>

Check the state in **Dashboard > AgentKit > Connected accounts**:

| State | What you do |
|-------|-------------|
| `PENDING_AUTH` | Open the magic link and finish OAuth |
| `PENDING_VERIFICATION` | Complete [user verification](/agentkit/user-verification/) |
| `EXPIRED` | Open a new magic link. Scalekit cannot refresh a dead refresh token |
| `DISCONNECTED` | Reconnect from the Dashboard, then retry |

See [Manage connected accounts](/agentkit/connected-accounts/).

</details>

<details>
<summary>Should I use Virtual MCP instead?</summary>

No, not as the default. Hermes is a long-lived host. The skill calls `execute_tool` with client credentials. You do not mint a session token.

Virtual MCP is a Scalekit MCP URL plus a session token that your app mints. Hermes gateway and cron stay up for days. A bearer in `~/.hermes/.env` expires. There is no shipped remint hook.

Use Virtual MCP in your **app** for a short agent run (Claude Managed Agents, Mastra, CrewAI), or for a short `hermes chat` demo:

1. Create a Virtual MCP config in the Dashboard.
2. Mint a session token for one identifier.
3. Point Hermes at the static `mcp_server_url` with `Authorization: Bearer ${SCALEKIT_MCP_SESSION_TOKEN}`.
4. Start a new chat before the token expires.

Do not set `auth: oauth` on the Virtual MCP server.

</details>

<details>
<summary>Why does hermes mcp login fail against Scalekit?</summary>

`hermes mcp login` is for vendor MCP OAuth. Scalekit Virtual MCP is a static URL plus a session token that **your app** mints.

Use the `scalekit-agent-auth` skill, or set a bearer header as in [Set up a Virtual MCP server](/agentkit/mcp/configure-mcp-server/).

</details>

<details>
<summary>When do I not need Scalekit?</summary>

Skip Scalekit when no named user is involved:

- GitHub work through `gh` on the operator's own machine
- Your own Linear account through the connector Hermes ships (`hermes mcp install linear`)
- Slack used only as a chat channel, through the Hermes Slack gateway

Add Scalekit when Hermes must act as a specific person: read Gmail as `alice`, post to Slack as that person, or write to that person's Google Calendar.

</details>
