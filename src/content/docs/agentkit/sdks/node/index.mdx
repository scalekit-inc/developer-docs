---
title: 'Node.js SDK'
description: 'Install and initialize the Scalekit Node.js SDK for AgentKit.'
topic: agentkit-sdks
sidebar:
  label: 'Installation'
  order: 1
tableOfContents: false
---

`scalekit.actions` is the primary interface for AgentKit. It handles connected account management, tool execution, and proxied API calls. `scalekit.tools` exposes raw tool schemas for building custom adapters.

Use the left sidebar to open **Actions**, **Tools**, and **Errors**.

## Install

```bash
npm install @scalekit-sdk/node
```

## Initialize

```ts wrap showLineNumbers=false
import { ScalekitClient } from '@scalekit-sdk/node'

const scalekit = new ScalekitClient(
  process.env.SCALEKIT_ENVIRONMENT_URL!,
  process.env.SCALEKIT_CLIENT_ID!,
  process.env.SCALEKIT_CLIENT_SECRET!
)
```

## Next steps

- [Actions](/agentkit/sdks/node/actions/) — auth links, connected accounts, tool execution  
- [Tools](/agentkit/sdks/node/tools/) — raw tool schemas  
- [Errors](/agentkit/sdks/node/errors/) — exception types  
