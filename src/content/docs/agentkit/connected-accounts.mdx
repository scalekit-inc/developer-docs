---
title: Manage connected accounts
description: Check status, list, delete, and update scopes for connected accounts.
tags: [agentauth, connectedaccounts, oauth2, api, guide]
sidebar:
  order: 4
  label: "Manage connected accounts"
tableOfContents:
  maxHeadingLevel: 3
prev:
  label: "Verify user identity"
  link: "/agentkit/user-verification"
next:
  label: "Bring your own credentials"
  link: "/agentkit/advanced/bring-your-own-oauth"
---

import { Steps, Tabs, TabItem, Aside } from '@astrojs/starlight/components';

A **connected account** is the per-user record that holds a user's tokens and tracks their authorization state for a specific connection. Scalekit creates one automatically when a user completes the OAuth flow.

## Account states

| State | Meaning |
|---|---|
| `PENDING` | User hasn't completed authentication |
| `ACTIVE` | Tokens valid — ready for tool calls |
| `EXPIRED` | Tokens expired — re-authorization required |
| `REVOKED` | User revoked access from the provider |
| `ERROR` | Authentication or configuration error |

## Check account status

<Tabs syncKey="tech-stack">
  <TabItem label="Python">
```python
response = actions.get_connected_account(
    connection_name="gmail",
    identifier="user_123"
)
connected_account = response.connected_account
print(f"Status: {connected_account.status}")
```
  </TabItem>
  <TabItem label="Node.js">
```typescript
const response = await actions.getConnectedAccount({
  connectionName: 'gmail',
  identifier: 'user_123',
});

console.log('Status:', response.connectedAccount?.status);
```
  </TabItem>
</Tabs>

## Re-authorize when tokens expire

When a connected account isn't `ACTIVE`, generate a new authorization link and prompt the user to re-authorize. The flow is the same as initial authorization — see [Authorize a user](/agentkit/tools/authorize/).

<Tabs syncKey="tech-stack">
  <TabItem label="Python">
```python
if connected_account.status != "ACTIVE":
    link_response = actions.get_authorization_link(
        connection_name="gmail",
        identifier="user_123"
    )
    # Redirect or send link_response.link to the user
```
  </TabItem>
  <TabItem label="Node.js">
```typescript
if (connectedAccount?.status !== 'ACTIVE') {
  const linkResponse = await actions.getAuthorizationLink({
    connectionName: 'gmail',
    identifier: 'user_123',
  });
  // Redirect or send linkResponse.link to the user
}
```
  </TabItem>
</Tabs>

## List connected accounts

<Aside type="note" title="Node.js only">
  List and delete operations are currently available in the Node.js SDK. Use the [Scalekit dashboard](https://app.scalekit.com) or REST API for Python.
</Aside>

```typescript
const listResponse = await actions.listConnectedAccounts({
  connectionName: 'gmail',
});
console.log('Connected accounts:', listResponse);
```

## Delete a connected account

Deleting a connected account removes the user's tokens and authorization state. The user must re-authorize to reconnect.

```typescript
await actions.deleteConnectedAccount({
  connectionName: 'gmail',
  identifier: 'user_123',
});
```

## Update scopes

Scopes are set at the connection level. To request additional permissions from an existing connected account:

<Steps>
1. Update the connection's scopes in **Agent Auth > Connections > Edit**.
2. Generate a new authorization link for the user.
3. The user completes the OAuth consent screen, approving the updated scopes.
4. Scalekit updates the connected account with the new token set.
</Steps>

The user must go through the OAuth flow again whenever scopes change.
