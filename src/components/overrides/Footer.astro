---
import Default from '@astrojs/starlight/components/Footer.astro'
import CustomFooter from '@components/ui/CustomFooter.astro'
const showCustomFooter = Astro.url.pathname === '/'
---

{!showCustomFooter && <Default {...Astro.props} />}
{showCustomFooter && <CustomFooter />}

<style>
  /* Styles live in CustomFooter.astro */
</style>
