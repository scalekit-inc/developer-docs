---
import FoldHeader from '../FoldHeader.astro'
import CardGrid from '../CardGrid.astro'
import FoldCard from '../FoldCard.astro'
import SharedStyles from './SharedStyles.astro'
import IconLucideUsers from '~icons/lucide/users'
import IconLucideMail from '~icons/lucide/mail'
import IconLucideKeyRound from '~icons/lucide/key-round'
import IconLucideArrowLeftRight from '~icons/lucide/arrow-left-right'
import IconStreamlineUserSyncOnlineInPersonRemix from '~icons/streamline/user-sync-online-in-person-remix'
import { Badge } from '@astrojs/starlight/components'

/* Plug n Play – Auth Modules */
---

<SharedStyles />

<section id="auth-for-humans" class="fold-section fold--background-muted fold-full-width">
  <div class="home-container">
    <div class="fold-container">
      <div>
        <FoldHeader
          title="For App Developers"
          description="Explore our authentication solutions and see how they can transform your application security"
          descriptionSize="1rem"
          titleSize="2rem"
          titleWeight="500"
          titleColor="var(--sl-color-text-heading)"
          className="auth-modules-header"
        />
        <FoldCard
          title="Full-Stack Auth"
          icon={IconLucideUsers}
          href="/fsa/quickstart"
          cta="Get started →"
        >
          <p class="card-desc">
            Complete solution for authentication, session and user management in one seamless
            package.
            <div>
              <Badge text="Session Management" />
              <Badge text="Hosted Login/Signup pages" />
              <Badge text="User Management" />
              <Badge text="Roles & Permissions" />
              <Badge text="Auth Methods" />
            </div>
          </p>
        </FoldCard>
        <div class="my-6 flex items-center gap-3">
          <span class="h-px flex-1 bg-[var(--sl-color-gray-5)]" aria-hidden="true"></span>
          <span class="text-sm text-[var(--sl-color-gray-3)] uppercase">OR</span>
          <span class="h-px flex-1 bg-[var(--sl-color-gray-5)]" aria-hidden="true"></span>
        </div>
        <FoldHeader
          title="Have your own existing Auth setup?"
          description=""
          descriptionSize="1rem"
          titleSize="1.25rem"
          titleWeight="500"
          titleColor="var(--sl-color-text-heading)"
          className="auth-modules-header"
        />
        <CardGrid className="feature-cards-grid">
          <FoldCard
            title="Single Sign-on"
            icon={IconLucideKeyRound}
            href="/sso/quickstart"
            cta="Quick setup →"
          >
            <p class="card-desc">
              Add SSO to your SaaS app in hours and make enterprise logins easy for your customers,
              with no extra setup.
            </p>
          </FoldCard>
          <FoldCard
            title="SCIM Provisioning"
            icon={IconStreamlineUserSyncOnlineInPersonRemix}
            href="/directory/scim/quickstart"
            cta="Quick setup →"
          >
            <p class="card-desc">
              Automate user provisioning and deprovisioning through a unified interface, keeping
              your app’s user access always up to date.
            </p>
          </FoldCard>
          <FoldCard
            title="Passwordless Authentication"
            icon={IconLucideMail}
            href="/passwordless/quickstart"
            cta="Quick setup →"
          >
            <p class="card-desc">
              Offer users a seamless passwordless login experience for your app with magic links and
              verification codes.
            </p>
          </FoldCard>
        </CardGrid>
      </div>
    </div>
  </div>
</section>

<style>
  .sl-badge {
    font-family: inherit;
    border-width: 1px; /* match FoldCard (1px) */
    border-color: #cecece;
    padding-inline: 0.5rem;
  }
</style>
