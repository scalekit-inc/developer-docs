---
import { Card as StarlightCard } from '@astrojs/starlight/components'
import '../../styles/folds.css'

export interface Props {
  title: string
  href?: string
}

const { title, href } = Astro.props
---

<StarlightCard title={title} class:list="fold-card">
  <slot />
  {
    href && (
      <a href={href} class="card-cta">
        Know more →
      </a>
    )
  }
</StarlightCard>
