import * as CB from '@gesslar/starlight-class-browser'

<CB.ClassBrowser
  name="WebAuthn"
  type="client"
  source="https://github.com/scalekit-inc/scalekit-sdk-node/blob/main"
  language="typescript"
>

  <CB.ClassMethod name="listCredentials">
    List all WebAuthn credentials for a user.

  </CB.ClassMethod>

```typescript
const response = await scalekitClient.webauthn.listCredentials('usr_123456');
console.log('Credentials:', response.credentials);
```

  <CB.ClassMethod name="updateCredential">
    Update a WebAuthn credential's display name.

  </CB.ClassMethod>

```typescript
const response = await scalekitClient.webauthn.updateCredential(
  'cred_123',
  'My YubiKey'
);
```

  <CB.ClassMethod name="deleteCredential">
    Delete a WebAuthn credential.

  </CB.ClassMethod>

```typescript
const response = await scalekitClient.webauthn.deleteCredential('cred_123');
console.log('Deleted:', response.success);
```

</CB.ClassBrowser>
