import * as CB from '@gesslar/starlight-class-browser'

<CB.ClassBrowser
  name="Domains"
  type="client"
  source="https://github.com/scalekit-inc/scalekit-sdk-java/blob/main"
  language="java"
>

  <CB.ClassMethod name="createDomain">
    Creates a domain for an organization.

  </CB.ClassMethod>

```java
scalekitClient.domains().createDomain("org_123", "acme.com");
```

  <CB.ClassMethod name="createDomain">
    Creates a domain for an organization with a specified domain type.

  </CB.ClassMethod>

```java
import com.scalekit.grpc.scalekit.v1.domains.DomainType;

scalekitClient.domains().createDomain("org_123", "acme.com", DomainType.DOMAIN_TYPE_PRIMARY);
```

  <CB.ClassMethod name="createDomain">
    Creates a domain using a request object.

  </CB.ClassMethod>

```java
import com.scalekit.grpc.scalekit.v1.domains.CreateDomainRequest;

CreateDomainRequest req = CreateDomainRequest.newBuilder()
  .setOrganizationId("org_123")
  .setDomain("acme.com")
  .build();

scalekitClient.domains().createDomain(req);
```

  <CB.ClassMethod name="getDomainById">
    Gets a domain by ID for an organization.

  </CB.ClassMethod>

```java
scalekitClient.domains().getDomainById("org_123", "dom_123");
```

  <CB.ClassMethod name="listDomainsByOrganizationId">
    Lists domains for an organization.

  </CB.ClassMethod>

```java
scalekitClient.domains().listDomainsByOrganizationId("org_123");
```

  <CB.ClassMethod name="listDomainsByOrganizationId">
    Lists domains for an organization filtered by domain type.

  </CB.ClassMethod>

```java
import com.scalekit.grpc.scalekit.v1.domains.DomainType;

scalekitClient.domains().listDomainsByOrganizationId("org_123", DomainType.DOMAIN_TYPE_PRIMARY);
```

  <CB.ClassMethod name="deleteDomain">
    Deletes a domain by ID for an organization.

  </CB.ClassMethod>

```java
scalekitClient.domains().deleteDomain("org_123", "dom_123");
```

</CB.ClassBrowser>
