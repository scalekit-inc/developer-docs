import * as CB from '@gesslar/starlight-class-browser'

<CB.ClassBrowser
  name="Passwordless"
  type="client"
  source="https://github.com/scalekit-inc/scalekit-sdk-python/blob/main"
  language="python"
>

  <CB.ClassMethod name="send_passwordless_email">
    Send a passwordless authentication email with OTP or magic link.

  </CB.ClassMethod>

```python
response = scalekit_client.passwordless.send_passwordless_email(
    'user@example.com',
    template='SIGNIN',
    state='random-state',
    expires_in=3600
)

print(f'Auth Request ID: {response[0].auth_request_id}')
```

  <CB.ClassMethod name="verify_passwordless_email">
    Verify a passwordless authentication code or link token.

  </CB.ClassMethod>

```python
response = scalekit_client.passwordless.verify_passwordless_email(
    code='123456',
    auth_request_id='auth_request_id'
)

print(f'Email: {response[0].email}')
```

  <CB.ClassMethod name="resend_passwordless_email">
    Resend a passwordless authentication email.

  </CB.ClassMethod>

```python
response = scalekit_client.passwordless.resend_passwordless_email('auth_request_id')
```

</CB.ClassBrowser>
