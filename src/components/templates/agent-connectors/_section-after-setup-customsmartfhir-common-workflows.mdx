{/* TODO: stub cloned from _section-after-setup-hubspot-common-workflows.mdx for  SMART App on FHIR. Review and update connector-specific references (URLs, scopes, app-registration steps) before merging. */}
export const sectionTitle = 'Common workflows'

import { Tabs, TabItem } from '@astrojs/starlight/components'

<details>
<summary>Proxy API call</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const result = await actions.request({
      connectionName: 'customsmartfhir',
      identifier: 'user_123',
      path: '/crm/v3/owners',
      method: 'GET',
    });
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    result = actions.request(
        connection_name='customsmartfhir',
        identifier='user_123',
        path="/crm/v3/owners",
        method="GET"
    )
    print(result)
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Create a contact</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const contact = await actions.executeTool({
      connector: 'customsmartfhir',
      identifier: 'user_123',
      toolName: 'customsmartfhir_contact_create',
      toolInput: {
        email: 'jane.smith@acme.com',
        firstname: 'Jane',
        lastname: 'Smith',
        jobtitle: 'VP of Engineering',
        company: 'Acme Corp',
        lifecyclestage: 'lead',
      },
    });
    console.log('Created contact ID:', contact.id);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    contact = actions.execute_tool(
        connection_name='customsmartfhir',
        identifier='user_123',
        tool_name="customsmartfhir_contact_create",
        tool_input={
            "email": "jane.smith@acme.com",
            "firstname": "Jane",
            "lastname": "Smith",
            "jobtitle": "VP of Engineering",
            "company": "Acme Corp",
            "lifecyclestage": "lead",
        },
    )
    print("Created contact ID:", contact["id"])
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Search deals</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const deals = await actions.executeTool({
      connector: 'customsmartfhir',
      identifier: 'user_123',
      toolName: 'customsmartfhir_deals_search',
      toolInput: {
        query: 'enterprise',
        filterGroups: JSON.stringify([{
          filters: [{ propertyName: 'dealstage', operator: 'EQ', value: 'qualifiedtobuy' }]
        }]),
        properties: 'dealname,amount,dealstage,closedate',
        limit: 10,
      },
    });
    console.log('Found deals:', deals.results);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import json

    deals = actions.execute_tool(
        connection_name='customsmartfhir',
        identifier='user_123',
        tool_name="customsmartfhir_deals_search",
        tool_input={
            "query": "enterprise",
            "filterGroups": json.dumps([{
                "filters": [{"propertyName": "dealstage", "operator": "EQ", "value": "qualifiedtobuy"}]
            }]),
            "properties": "dealname,amount,dealstage,closedate",
            "limit": 10,
        },
    )
    print("Found deals:", deals["results"])
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Log a call</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const call = await actions.executeTool({
      connector: 'customsmartfhir',
      identifier: 'user_123',
      toolName: 'customsmartfhir_call_log',
      toolInput: {
        hs_call_title: 'Q4 Renewal Discussion',
        hs_timestamp: new Date().toISOString(),
        hs_call_body: 'Discussed renewal terms. Customer is interested in the enterprise plan.',
        hs_call_direction: 'OUTBOUND',
        hs_call_duration: 900000, // 15 minutes in ms
        hs_call_status: 'COMPLETED',
      },
    });
    console.log('Logged call ID:', call.id);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    from datetime import datetime, timezone

    call = actions.execute_tool(
        connection_name='customsmartfhir',
        identifier='user_123',
        tool_name="customsmartfhir_call_log",
        tool_input={
            "hs_call_title": "Q4 Renewal Discussion",
            "hs_timestamp": datetime.now(timezone.utc).isoformat(),
            "hs_call_body": "Discussed renewal terms. Customer is interested in the enterprise plan.",
            "hs_call_direction": "OUTBOUND",
            "hs_call_duration": 900000,  # 15 minutes in ms
            "hs_call_status": "COMPLETED",
        },
    )
    print("Logged call ID:", call["id"])
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Create and associate a ticket</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    // Create the ticket
    const ticket = await actions.executeTool({
      connector: 'customsmartfhir',
      identifier: 'user_123',
      toolName: 'customsmartfhir_ticket_create',
      toolInput: {
        subject: 'Cannot export data to CSV',
        hs_pipeline_stage: '1', // "New" stage
        content: 'Customer reports that the CSV export button is unresponsive on the Reports page.',
        hs_ticket_priority: 'HIGH',
      },
    });

    // Associate with a contact
    await actions.executeTool({
      connector: 'customsmartfhir',
      identifier: 'user_123',
      toolName: 'customsmartfhir_association_create',
      toolInput: {
        from_object_type: 'tickets',
        from_object_id: ticket.id,
        to_object_type: 'contacts',
        to_object_id: '12345',
      },
    });
    console.log('Ticket created and associated:', ticket.id);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    # Create the ticket
    ticket = actions.execute_tool(
        connection_name='customsmartfhir',
        identifier='user_123',
        tool_name="customsmartfhir_ticket_create",
        tool_input={
            "subject": "Cannot export data to CSV",
            "hs_pipeline_stage": "1",  # "New" stage
            "content": "Customer reports that the CSV export button is unresponsive on the Reports page.",
            "hs_ticket_priority": "HIGH",
        },
    )

    # Associate with a contact
    actions.execute_tool(
        connection_name='customsmartfhir',
        identifier='user_123',
        tool_name="customsmartfhir_association_create",
        tool_input={
            "from_object_type": "tickets",
            "from_object_id": ticket["id"],
            "to_object_type": "contacts",
            "to_object_id": "12345",
        },
    )
    print("Ticket created and associated:", ticket["id"])
    ```
  </TabItem>
</Tabs>

</details>