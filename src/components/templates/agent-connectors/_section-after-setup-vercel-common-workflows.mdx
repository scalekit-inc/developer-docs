export const sectionTitle = 'Common workflows'

import { Tabs, TabItem } from '@astrojs/starlight/components'

<details>
<summary>Proxy API call</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const result = await actions.request({
      connectionName: 'vercel',
      identifier: 'user_123',
      path: '/v2/user',
      method: 'GET',
    });
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    result = actions.request(
        connection_name='vercel',
        identifier='user_123',
        path="/v2/user",
        method="GET"
    )
    print(result)
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Execute a tool</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const result = await actions.executeTool({
      connector: 'vercel',
      identifier: 'user_123',
      toolName: 'vercel_alias_create',
      toolInput: {},
    });
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    result = actions.execute_tool(
        tool_input={},
        tool_name='vercel_alias_create',
        connection_name='vercel',
        identifier='user_123',
    )
    print(result)
    ```
  </TabItem>
</Tabs>

</details>
