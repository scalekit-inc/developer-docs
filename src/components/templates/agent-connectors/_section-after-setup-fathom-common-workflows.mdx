export const sectionTitle = 'Common workflows'

import { Tabs, TabItem } from '@astrojs/starlight/components'

<details>
<summary>List past meetings</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const result = await actions.executeTool({
      connector: 'fathom',
      identifier: 'user_123',
      toolName: 'fathom_list_meetings',
      toolInput: {},
    });
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    result = actions.execute_tool(
        connection_name='fathom',
        identifier='user_123',
        tool_name='fathom_list_meetings',
        tool_input={},
    )
    print(result)
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Get a recording transcript</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const result = await actions.executeTool({
      connector: 'fathom',
      identifier: 'user_123',
      toolName: 'fathom_get_recording_transcript',
      toolInput: { recording_id: 12345 },
    });
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    result = actions.execute_tool(
        connection_name='fathom',
        identifier='user_123',
        tool_name='fathom_get_recording_transcript',
        tool_input={'recording_id': 12345},
    )
    print(result)
    ```
  </TabItem>
</Tabs>

</details>

<details>
<summary>Create a webhook subscription</summary>

<Tabs syncKey="tech-stack">
  <TabItem label="Node.js">
    ```typescript
    const result = await actions.executeTool({
      connector: 'fathom',
      identifier: 'user_123',
      toolName: 'fathom_create_webhook',
      toolInput: {
        destination_url: 'https://yourapp.example.com/webhooks/fathom',
        triggered_for: ['my_recordings'],
        include_summary: true,
        include_transcript: true,
      },
    });
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    result = actions.execute_tool(
        connection_name='fathom',
        identifier='user_123',
        tool_name='fathom_create_webhook',
        tool_input={
            'destination_url': 'https://yourapp.example.com/webhooks/fathom',
            'triggered_for': ['my_recordings'],
            'include_summary': True,
            'include_transcript': True,
        },
    )
    print(result)
    ```
  </TabItem>
</Tabs>

</details>
