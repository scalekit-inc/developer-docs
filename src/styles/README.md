# Fold styles

`folds.css` holds shared layout for `FoldCard` and pages that use `.fold-section` (SaaSKit home, SDK landing).

Background tokens `--home-bg-plain`, `--home-bg-muted`, and `--home-bg-alternate` live in this file. Do not add homepage Fold1–Fold5 components.
