# Fold styles

`folds.css` holds shared layout for `FoldCard` and pages that use `.fold-section` (SaaSKit home, SDK landing).

`FoldCard.astro` imports `folds.css`. Do not add homepage Fold1–Fold5 components. Those landing folds are gone.
