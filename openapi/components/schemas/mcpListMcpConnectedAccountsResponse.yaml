type: object
properties:
  connected_accounts:
    description: Connected account state for each connection in the configuration
    type: array
    items:
      type: object
      $ref: ./mcpMcpConnectionAuthState.yaml
