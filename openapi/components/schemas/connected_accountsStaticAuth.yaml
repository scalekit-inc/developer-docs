type: object
title: Static authentication credentials for API keys, bearer tokens, or basic auth
properties:
  details:
    description: >-
      Flexible JSON structure containing static credentials. Format varies by
      connector type (API key, username/password, etc.).
    type: object
    examples:
      - api_key: sk_live_...
        api_secret: ...
