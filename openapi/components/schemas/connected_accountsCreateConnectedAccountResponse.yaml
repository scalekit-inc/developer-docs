type: object
properties:
  connected_account:
    description: >-
      The newly created connected account with its unique identifier, status,
      and complete authorization details including access tokens.
    $ref: ./connected_accountsConnectedAccount.yaml
