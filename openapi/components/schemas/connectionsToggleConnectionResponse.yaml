type: object
properties:
  enabled:
    description: >-
      Current state of the connection after the operation. True means the
      connection is now enabled and can be used for authentication.
    type: boolean
    examples:
      - true
  error_message:
    description: Error message if the operation fails
    type: string
    examples:
      - placeholder
