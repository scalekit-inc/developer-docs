description: A documentation or reference link.
type: object
properties:
  description:
    description: Human-readable label for the link (e.g. "User verification flow").
    type: string
  url:
    description: >-
      Absolute URL to the documentation page (e.g.
      "https://docs.scalekit.com/...").
    type: string
