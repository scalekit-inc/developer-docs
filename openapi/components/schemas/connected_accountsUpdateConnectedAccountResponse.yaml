type: object
properties:
  connected_account:
    description: >-
      The updated connected account with refreshed credentials, new token
      expiry, and modified configuration settings.
    $ref: ./connected_accountsConnectedAccount.yaml
