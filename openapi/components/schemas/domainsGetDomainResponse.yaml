type: object
properties:
  domain:
    description: >-
      The requested domain object with complete details including domain type,
      timestamps and configuration.
    $ref: ./domainsDomain.yaml
