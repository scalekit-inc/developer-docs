type: object
properties:
  revoked_session:
    description: >-
      Details of the revoked session including session ID, user ID, creation and
      revocation timestamps, and final device information.
    $ref: ./sessionsRevokedSessionDetails.yaml
