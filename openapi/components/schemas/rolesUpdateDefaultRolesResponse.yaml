type: object
properties:
  default_creator:
    description: >-
      The role that is now set as the default creator role for the environment.
      Contains complete role information including permissions and metadata.
    $ref: ./v1rolesRole.yaml
    examples:
      - description: Role for creating resources
        display_name: Creator Role
        id: role_1234567890
        name: creator
  default_member:
    description: >-
      The role that is now set as the default member role for the environment.
      Contains complete role information including permissions and metadata.
    $ref: ./v1rolesRole.yaml
    examples:
      - description: Role for regular members
        display_name: Member Role
        id: role_0987654321
        name: member
