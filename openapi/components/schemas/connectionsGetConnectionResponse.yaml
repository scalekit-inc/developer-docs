type: object
properties:
  connection:
    description: >-
      Complete connection details including provider configuration, protocol
      settings, status, and all metadata. Contains everything needed to
      understand the connection's current state.
    $ref: ./connectionsConnection.yaml
