type: object
properties:
  config_name:
    description: New MCP configuration name to attach to the instance
    type: string
    examples:
      - daily-summarizer
  name:
    description: New display name for the MCP instance
    type: string
    examples:
      - daily-digest-updated
