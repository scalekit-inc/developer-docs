type: object
properties:
  connected_account:
    description: >-
      The connected account with complete details including sensitive
      authorization credentials (access tokens, refresh tokens, scopes). Handle
      with appropriate access controls.
    $ref: ./connected_accountsConnectedAccount.yaml
