[build]
  command = "curl -fsSL https://d2lang.com/install.sh | sh -s -- --prefix=$HOME/.local && export PATH=$HOME/.local/bin:$PATH && pnpm build"
  publish = "dist"

[build.environment]
  PATH = "/opt/buildhome/.local/bin:$PATH"

[[plugins]]
  package = "@astrojs/netlify"
